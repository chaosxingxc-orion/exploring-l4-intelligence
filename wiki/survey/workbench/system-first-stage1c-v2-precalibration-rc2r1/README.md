# Stage-1C v2 Agentic RC2R1 workbench

This workbench is the immutable-successor repair to the committed Agentic RC2 package. RC2 remains
bound to commit `74cf8e4b565a9e53ff40f9dbc34961ede853dd57`; RC2R1 does not rewrite or migrate it.

RC2R1 closes only the bounded independent-review defects:

- exact N=56, exact canonical-ID, completed-response and provenance-bound agreement intake;
- independent agreement gates for every paper and critical object field;
- structured and mutually exclusive REFERENCE, BORROW_PROTOCOL and REPRODUCTION_CANDIDATE evidence;
- independent official ACL receipts for the two inherited ACL calibration sources;
- recursive forbidden-key and known-label value scanning over every coder-visible metadata artifact.

The package is `AGENTIC_RC2R1_CODER_READY_NOT_DISTRIBUTED`. Independent acceptance is required before
the already authorized conditional distribution may bind real coder identities, process receipts and
the disclosed owner adjudicator. No agreement, mapping or research execution is present here.

Run the deterministic checker with:

```text
python scripts/survey/sf_stage1c_v2_precalibration_rc2r1.py
```

The exact reviewer surface is `review-package-manifest-rc2r1.json`.
