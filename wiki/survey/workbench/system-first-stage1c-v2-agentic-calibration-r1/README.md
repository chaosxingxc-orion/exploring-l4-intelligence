# Stage-1C v2 Agentic calibration round 1

Status: `AGREEMENT_FAILED_OWNER_CONSOLIDATION_DECISION_REQUIRED`.

The exact RC2R3 method package was accepted at commit
`ee12a3a79bd578df996c44c4bdb2dbc709e2f616`. Two fresh no-fork model coders received byte-identical
eight-artifact bundles after receiver-side raw-byte verification:

- A: `gpt-5.6-sol`, task `/root/stage1c_coder_a_r1`;
- B: `gpt-5.6-terra`, task `/root/stage1c_coder_b_r1`.

Both use separate no-`.git` workspaces and the same verified 135-rendition source corpus. They may not
access the umbrella repository, network discovery, prior labels or one another's outputs. These are
two isolated model coders, not provider-independent or human inter-rater evidence.

`delivery-receipts.json` and `runtime-intake-bound.json` are frozen distribution evidence. Both exact
N=56 outputs passed schema and semantic validation, then their raw bytes were frozen before raw
pre-adjudication agreement was computed. The result is `FAIL`: 5/13 paper-level paths pass, 8 fail,
and all nine object types have zero cross-coder match keys. No adjudication has been applied.

Read the detailed Chinese owner package:
[`stage1c-v2-calibration-r1-owner-decision-package-zh.md`](stage1c-v2-calibration-r1-owner-decision-package-zh.md).
Owner authorization is required before the one permitted codebook consolidation and full N=56
recode by two new isolated contexts. The later mapping signature remains mandatory.

No research model, benchmark metric, paper reproduction, prototype, novelty verdict, mapping,
Stage-2A or push is authorized by this distribution.
