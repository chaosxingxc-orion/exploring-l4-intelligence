# Stage-1C v2 Agentic calibration R2R1

Status: `AGENTIC_CALIBRATION_R2R1_METHOD_READY_NOT_DISTRIBUTED`

R2R1 is the single bounded successor authorized by
`AUTHORIZE_STAGE1C_V2_AGENTIC_CALIBRATION_R2R1_BOUNDED_METHOD_REPAIR`. It preserves the exact R2
N=56 identities, 38 overlays, 18 sentinels and source renditions. R1 raw outputs/agreement and the
R2 method package remain immutable.

R2R1 closes only the three defects reproduced by the R2 independent review:

1. coder-local IDs are unique within each object type, all maps are typed and compatibility targets
   declare their object type before deterministic compiler resolution;
2. every paper-visible reproduction fact carries an explicit observability state, and closed support
   requires all ten facts to be affirmatively observed with an immutable revision and non-ambiguous
   access; and
3. each coder submission freezes one canonical UTF-8 JSON byte array under a submission receipt.
   Both receipt hashes and response digests enter runtime intake and a frozen response root that the
   agreement engine recomputes before response validation or metrics.

The coder-visible bundle remains exactly eight artifacts. Submission receipts, runtime intake,
positive-support evidence, local readiness and expected labels remain reviewer/runtime-only. No
R2R1 coder packet may be distributed until a fresh exact independent review returns
`ACCEPT_AGENTIC_CALIBRATION_R2R1_METHOD_CONTRACT_FOR_CODER_INTAKE`.

This package authorizes no agreement, adjudication, 320-work mapping, research model, benchmark,
reproduction, prototype, novelty verdict, Stage-2A or push.
