# Stage-1C v2 Agentic calibration R2

Status: `AGENTIC_CALIBRATION_R2_METHOD_READY_NOT_DISTRIBUTED`

R2 is the single bounded successor authorized by
`AUTHORIZE_STAGE1C_V2_AGENTIC_CALIBRATION_R1_CODEBOOK_CONSOLIDATION`. It preserves the exact R1
N=56 sample and source renditions. R1 raw outputs, raw agreement and registered audit transactions
remain immutable.

R2 closes four method-contract defects before the only permitted recode:

1. coders no longer emit `object_match_key`; the compiler derives source anchors, resolves
   cross-object references and creates deterministic segmentation signatures;
2. unmatched objects enter the union denominator for segmentation and every applicable critical
   field; `NOT_CALIBRATED` is reserved for a true both-zero class;
3. blind `paper_reproduction_support` is separated from reviewer-only
   `local_reproduction_readiness`; a paper support record may remain `OPEN_WITH_BLOCKERS` without
   pretending to be a reproduction candidate or anchor; and
4. reviewer-only positive-support preflight proves that the unchanged packet can express two TRACE
   dataset edges and one AudioToolAgent paper-support record. Expected labels are not distributed.

The coder-visible bundle remains eight byte-identical artifacts. The local-readiness ledger,
positive-support ledger and preflight are reviewer-only. No R2 coder is distributed until a fresh
independent exact-manifest review returns
`ACCEPT_AGENTIC_CALIBRATION_R2_METHOD_CONTRACT_FOR_CODER_INTAKE`.

This package authorizes no 320-work mapping, benchmark, reproduction, prototype, novelty verdict,
Stage-2A or push.
