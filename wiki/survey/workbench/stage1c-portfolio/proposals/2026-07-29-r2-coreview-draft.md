---
artifact_id: "SF-STAGE1C-R2-COREVIEW-V12"
role: "R2 开题报告 v12：十六轮裁决重构版（v11）关闭 round-10 全部 6 MAJOR + 16 MINOR——机制核消融臂与 K-PS/K-NB 判定装置补齐、RE_SLICE 估值、门一全路由、自足性符号清零、四节可读性恢复"
status: "V12_PENDING_ROUND11_REREVIEW; owner 未签"
template: "2026-07-29-direction-coreview-template.md (V2) + Decision-Log 续79/80/81 系列裁定"
review_chain: "round-03..10 八轮评审见 wiki/audit/system-first-stage1c-v2/（round-10 对 v11 全量重审判 6 MAJOR+16 MINOR，本版逐条关闭，回应信在 round-10/）；D2 深读条目=d2-entries/（33 篇）"
rulings: "Decision-Log 续76/77/78/79/80/81"
evidence_cut: "2026-07-30"
supersedes: "V11（blob 3b3fc03d @ 94b24e2）与 V10（@ 50b029b）及更早；本件自足，不以任何已取代版本承重"
execution_authority: "STAGE2A_WITHHELD"
---

# R2 开题报告：语音任务中的知识系统——门控 agent 底盘上的 key/value 建模、组织与使用

## §0 一页摘要

**方向陈述（三层归位）**：**研究主题=知识**——语音任务中知识的 key/value 建模、组织、
供给与使用，及其基于任务效果的 training-free 端到端优化（TFRL）；**底盘=继承自文本域的
成熟 agent 架构**（ReAct/Reflexion/DeepAgents 谱系），我们的动作是把它**门控化**；
**战场=语音任务谱系**（从音频理解、口语问答到外部知识深搜，六族任务矩阵，§5.1）。

**核心主张（新基线，三臂可判）**：在实体/知识密集的语音任务上，**"语音内容知识 + 冻结
omni 核"可以替代"专用 ASR + biasing/GER"成为新的系统范式基线**——omni 核出原生语义
能力，知识系统补核的三个已知短板（实体：发音库+世界知识；副语言：面 key 联邦；长时程：
T2 库），门控环把两者缝合。判定=三臂对照：omni 裸核（下界）/ ASR+biasing-GER
（incumbent，必须赢的对手）/ omni 核+知识系统（主张臂）。

**三角色统一架构**：

```text
门控环（确定性触发电池 + V̂ 调度 + 准入门；TFRL 调参）——唯一调度权
  ├── 统一知识系统（研究主题）：对外一个查询接口，对内自决存量 or 现算——
  │     实体发音库（声学 key→同音候选 value）、T2 音频库（静态粗召回+动态细加工）、
  │     面 key 联邦（语义/情感/说话人/事件/时间锚）、世界知识接口；
  │     现算后端=冻结专家算子池（ASR/emotion2vec/说话人/事件/TTS/DSP，默认不跑、按需调用）；
  │     切片/面装载/融合权重/物化边界 = TFRL 优化的配置空间
  └── omni 裁决核：在"候选区已准入的证据 × 假设集"上裁决与作答
        （三不：不规划、不自省发起、不垄断感知——各有预检电池背书）
```

**运行范式**：门控化的**查询-裁决环**——一切动作（重听=查输入流、重切=查知识邻域、
检索=查外部、发音匹配=查发音库）都是对统一知识接口的**假设条件化查询**；何时问、问什么、
问多深由外置确定性门裁决（模型零自省）；一切查询输出落候选区、经准入门才进核上下文
（结构防爆总则）。

**任务三阶段**（三个研究问题簇+一个联合评价面，非时序串行）：

| 阶段 | 核心问题 | 章节 |
|---|---|---|
| 阶段一：谁搜索谁 | query 如何进入知识的地址空间？实体发音库机制核；个性化 | §2 |
| 阶段二：知识如何组织 | key 建模（切片×面×表示）与 value 建模；统一接口与两层绑定；TFRL 任务优化 | §3 |
| 阶段三：知识如何使用 | 门控查询-裁决环：双源触发、定向反思、准入与裁决 | §4 |

**研究纲领：效果优先**——主判据=相对最强基线的效果绝对提升；成本九维记账不设限；等预算/
Pareto 归后期。**Harness 学说**——每个 harness 组件带消融开关+蜕壳条件（model eats
harness）；可吃性分层：感知 harness 会被吃、门控部分被吃、**知识层结构上吃不掉（世界事实
晚于训练截止、时效、出处审计——文本域 RAG 在模型规模化中存活即证）——方向护城河钉在
知识层**，恰是研究主题所在。

**红线**：不修改任何模型参数；不为本任务新训练任何模型；不新增 LLM 代答（最终作答权在
冻结 omni 核）。工具级冻结组件（已发布检查点的 ASR/音频向量模型/TTS/判官/DSP/搜索引擎）
可用。**双门前置**：能力预检电池=Stage-2A 第零步（§6.1）；载体判定=先导数据集构造+现有
载体矩阵（§5）。判死永不在无分辨力的载体上宣布（灵敏度前置+回退梯，§5.3/§7）。

---

## §1 研究背景与问题定义

### §1.1 任务结构与两段式

R2 面向的任务因果链：**音频观测 → 音频语义假设 h（槽型任意：实体/事件/情感/说话人/声学
场景/音乐属性）→ 以 h 为钥匙的知识 → 有依据的回答**。知识检索是两段式：**定位**（key 的
职责——key 是地址不是副本，压缩即抽象，只需保留可判别面）→ **提取解析**（模型对 value 的
职责）。读集实证支撑两段拆分：SpeechRAG 检索侧几乎无损（0.9702 vs 0.9707）而生成端崩溃
（EM 0.3522 vs 0.7514）——定位成功、解析失败；Pi-Serini 的 surfaced 94.7→behavior 58.9
级联——证据在上下文里≠被使用。

### §1.2 音频特有机制与"why now"

**误差面（听错/判错）**：外部检索的 query 依赖可能听错的音频假设——把 Meyer 听成 Mayer，
检回的证据彼此一致、来源可信、条条锚在错误实体上，"多搜"反而把错误假设佐证得更牢
（Omni-DeepSearch A.6 over-search 轨迹；oracle 分解 entity-only 33.76/端到端 43.44/
gold-entity 50.00，仅 Gemini-3-Pro 口径、实体槽证据）。**结构面（同音异形）**：局部声学
片段自身不携带确定身份——ASR 的历史教训是身份由序列上下文与先验知识联合裁定（候选生成+
lattice 解码+LM rescore 的系统级方案）。

由此得本方向的机制核心：**把经典 ASR 三件套（声学模型-发音词典-语言模型）以开放知识系统
重新实例化**——声学 key（冻结编码器）扮演声学模型、实体发音库（声学 key→同音候选 value）
扮演发音词典、**LLM+外部世界知识扮演语义层 LM**（rescore 感知假设：世界上存在 Meyer 会议、
不存在 Mayer 会议）、门控 agent 环扮演解码器；且加入经典 ASR 没有的维度——说话人/情感等
面作为**索引维度**（经典 ASR 把说话人当噪声归一化掉）。**Why now（四使能件交集）**：能做
reasoning 级 rescore 的冻结 LLM（2023+）、成熟冻结专家编码器阵（emotion2vec 等）、agentic
控制范式（音频域 2026 起步）、评测端首次奖励分面理解（读集内〔cut 2026-07-30〕，omni
基准 2025+）；外加三个社区
（ASR 盯 WER/agentic 在文本/omni 做对齐）的目标函数都不指向该交点。类比：RAG 把知识从
LLM 权重外置；本方案把"发音→身份"映射从 ASR 权重外置——**实体层的 RAG-for-ASR，加分面
与 agentic**。具名近邻（占位补扫义务，§9）：contextual biasing（需每句预给候选表、训练
进解码器、单面）、GER/生成式纠错线（LLM 在 N-best 上纠错——回不到信号、无世界知识环）、
lattice 上的 spoken term detection（词层）、检索增强 ASR。

### §1.3 概念词典（件内自足）

**三形式（按被改变的系统对象互斥划分）**：**组织**——知识以什么单元/schema/关系/索引/
版本/出处存在（语料库、chunk/schema、索引、快照）；**供给**——何时取、从哪取、以什么
query 取、取多少、何时停（query 构造器、检索器、规划器、预算/停止策略）；**使用**——已
取回证据如何被接纳/融合/冲突处理/归因/拒用（准入、grounding、仲裁、abstention）。**两道门**：采集门（未见输出，决定买不买调用）≠准入门
（已见输出，决定进不进核上下文）。**四类信息作用**：①外部新信息（研究对象）②对已有观测
的重表达（重听属此类）③潜在知识激发（归 R1/R6）④验证与出处。**知识/记忆二分（作者性
判据）**：世界写的=知识（R2）；系统自己从 episode 写出的 revised lessons=记忆（R3）；
R2 只以 dev 预注册冻结先验消费记忆类信息，在线写入归 R3。**信息角色边界**：主张只覆盖
"外部新信息有边际价值"的实例分布；主载体按构造满足（过滤删除可直答样本），负类实例由
先导数据集按构造标注（§5.4）。

### §1.4 主张对象、红线与反还原不变量

北极星=system-level 任务能力，作答权在冻结 omni 核。红线三条见 §0。**反还原不变量**
（防"这只是 ASR rescoring"）：①目标函数永不含通用朗读语音 WER（实体/
知识密集集的实体准确率与该集 WER 可入榜）；②动作空间必须含无 ASR 类比的动作（定向重听、
按面重加工、双源比价）且其贡献可消融；③运行时 ASR 是 **harness 实验轴**而非身份——前向
通道 {omni 单通道 vs ASR+omni 双通道} 由实验裁决，系统身份由知识系统+门控环承载（被 TFRL
优化、被消融归因的部分）。**Harness 学说**：每个 harness 组件登记消融开关+蜕壳条件（核在
预检电池某项超阈值 X 后该组件应可摘除且效果不降——本身可验证）；可吃性分层与知识层护城河
见 §0/§8。

### §1.5 前置三问分型

**问题一：value 本体在哪（T1/T2，实验可行性分型）**——T1：value 在开放 web/文本（两个
现有主载体）；key 空间归搜索引擎，杠杆只在 query 侧；T2：value 本体是音频/受控库（锚数据
集与先导数据集）；key 表示才是可设计对象。跨分型不互推。**问题二：key 用什么表示**——由
冻结编码器联邦决定，选型判据=面的不变性/区分性需求（§2.4）。**问题三：什么装进 key、什么
装进 value**——key=可被检索的信息面（定位面设计），value=检索到的内容如何被使用（形态
服从使用端合同，§3.4）。

### §1.6 双门前置

**门一（能力门）**：模型是否具备预期的工具调用/裁决能力不作假设、只作测量——能力预检电池
=Stage-2A 第零步（§6.1），核的选择与前向通道配置由读数决定。**门二（载体门）**：是否有
现成数据集承载验证——逐阶段判定（§5）：阶段三 T1 载体存在但方向性（先分析基线再扩展）；
阶段二非语义面主张无现成载体（先导数据集构造，§5.4）。两门的答案都进报告正文，不作暗默
前提。

---

## §2 阶段一：谁搜索谁——映射、实体发音库与个性化

### §2.1 映射矩阵（读集内选项全景）

| query 模态 | key 空间（value 形态） | 代表工作 | 状态 |
|---|---|---|---|
| 文本 | 文本稀疏/稠密向量 | DPR 2004.04906 原理（key=E_P(标题⊕正文)，value 的确定性函数、单面） | 成熟 |
| 音频→文本 query | 搜索引擎自有 key | AudioRAG 2602.10656、Omni-DeepSearch 2605.08762 | 语音域现役（T1） |
| 文本 | 音频段向量（adapter 对齐） | SpeechRAG 2412.16500 | 研究线（T2） |
| 语音 | 语音向量 | SpeechDPR 2401.13463 | 研究线（T2） |
| 音频/文本混合 | 混合向量 | WavRAG 2502.14727 | 研究线（T2） |
| **实体音频 span** | **声学形式（多假设）** | **本方案实体发音库（§2.3）** | 提案 |

检索表征线三篇深读结论（数字经 LaTeX 源核对，D2 条目在册）：SpeechDPR top-20 19.73%
（级联 19.94%、集成 28.88%；未训练语音表征 0.04%——不可检索；gold passage 下 reader 上限
仅 11.17 FF1）；SpeechRAG 检索无损/生成崩（上引）、高 WER 区间端到端反超（45% WER：
0.7106→0.9952）；WavRAG 零训练 embedder 地板（R@1 0.3407/0.0675）、top-2→3 反降
（0.6408→0.5129）。三篇全有训练环节→方法论基线；**key 全部单向量单面、每 value 恰一 key**。

### §2.2 T1 的映射瓶颈与多假设 query

T1 上唯一可行路径=音频理解→文本 query→搜索引擎，瓶颈在 query 构造建立在可能听错的假设上。
杠杆=**多假设 query 构造**：候选假设并行发查、检索结果反向佐证假设（corr(e,h)，§6.3）。
跨域先例（红线内、确定性）：OK-VQA 的 ArticleNet 配方（内容词∪检测器标签→1/2 词组合枚举
→top-1 文章→规则抽句），语音同构=ASR 内容词∪面标签。

### §2.3 机制核：实体发音库

**构造**：从知识源拉实体清单→G2P/TTS 合成标准音（"国标发音"参考系）+**口音变体族**
（主要口音原型，方言学文献记录的系统性音变=世界知识）→冻结编码器产声学 key→库。key 为
**多假设 N-best 式**（不在建库时定案身份）。**查询**：疑似听错 span 的声学表示→检回同音/
近音候选实体集（value）→**世界知识 rescore 裁定**（哪个候选在世界中真实存在且语境相符）。
**范围钉死**：实体/术语层——通用逐字转写归冻结 ASR 工具（反还原不变量①）；中文同音字
密度使库价值更高。**与具名近邻的差异**：biasing 需每句预给候选表且训练进解码器，本库从
知识自动构造、检索式、冻结；GER 回不到信号、无世界知识，本方案反思时握原始音频重注意权
+发音库+世界知识。

### §2.4 个性化三件套（声学空间被说话人扭曲的对策）

①**对偶选型**：发音面 key 用说话人不变性编码器（对齐损失丢说话人信息在此恰是 feature），
说话人面 key 用区分性编码器（ECAPA 类）——同一"缺陷"在两个面分别是 bug 与 feature；
②**变体族 key**（静态层，见 §2.3）；③**查询期个性化 key 合成**（动态层）：冻结零样本
音色克隆 TTS 以当前说话人会话内音频为 enrollment，合成"这个人会怎么念候选实体"→个性化
声学 key→与实际发音匹配（把标准音推进说话人空间，而非把说话人拉回标准空间）；辅以会话内
对比归一化。**克隆的伦理与授权边界**：enrollment 音频仅限当前会话内当事说话人的任务音频、
不跨会话留存（留存即入 R3 且需另行授权）、克隆输出仅用于内部 key 合成比对、永不生成对外
播放的语音内容、不涉第三方 PII 合成——写入 authorization 合同（§9）。**R2/R3 界线**：
会话内瞬态说话人画像=R2；跨会话个人发音档案（revised lessons）=**R3 第一用例**。
**范围**：同语言口音变异可解；跨方言系统出界。

### §2.5 阶段一实验落点

A1（gold-entity ceiling，实体槽实例）与 A1′（gold-evidence 双读数：naive/masked+泄漏
占比——A-OKVQA 实测天真增益 60–71% 是答案泄漏）把映射损失与使用损失分开；发音库检索层
读数（同音候选命中率）独立报告；多假设 query 为供给侧 BASELINE VARIABLE。

---

## §3 阶段二：知识如何组织——key/value 建模与 TFRL 任务优化

### §3.1 文本域组织演进（D2 源核版）

**原理基座（pre-2024 时效例外）**：RAG 2005.11401 确立**外挂非参数记忆原理**——参数记忆
（生成器权重，原文中 BART 是联合微调的，"冻结生成器"是常见误读）与非参数记忆（稠密索引）
分离，索引可整体热替换：原文实测换任领导人索引后，与新索引同期的问题答对率 70%/68%、答出
错配旧答案的仅 12%/4%（82 题单模板单模型探针、同编码器同 schema 整索引替换，边界如实）；
它还在 2020 年就试过并放弃了"不检索"机制（null-document 三种实现全无增益），且观察到模型
用固定文档做 **no-op 检索伪装不检索**——该失效形态本方案在 §6.5 主动检出。DPR 2004.04906
确立稠密检索：两个训练对齐的编码器把文档与 query 映入同一空间、近邻即检索；其 key 是
value 的确定性函数、每 value 恰一个单面 key——"为同一 value 造多个信息不同的 key"要到
2024 年图/摘要索引一系才出现。ROME 2202.05262 证明参数中的事实**可直接编辑**，但编辑后
模型会编造貌似合理而无证据的新事实——知识外置的根本理由在**证据与出处**，不在可更新性。

**2024 年后的演进**：GraphRAG 2404.16130 在索引期用 LLM 预构建实体图+社区摘要，把跨文档
聚合前移到组织期——这是 **value 侧的层次化**（评测管线内没有摘要级 key 匹配，key 侧升级
被论文列为 future work）；其主语料之一是约 1M token 的 **Podcast 转写**——图组织已在口语
转写文本上验证过，但声学/副语言面零进入 key 或 value，文本域的组织升级止步于转写层。
HippoRAG 2405.14831 用名词短语图+PPR 实现**在线单跳成本**的多跳关联（省的只是在线侧、
离线索引反而更贵——成本从查询期搬到索引期）；其 uncertainty-based ensemble（阈值触发概念
面与稠密面分数融合）是文本域"多面 key+权重融合"的先例；其错误归因给出关键量化：**48% 的
失败源于 query 侧 key 只保留实体、丢弃上下文信号**——静态时刻决定保留什么，就在静态时刻
埋下失败。LightRAG 2410.05779 把索引产物换成逐节点/逐边 (K,V) 对、双层 key+双路 query
路由（构建成本无对比数字；D2 证据权重降级——directness 维度被删且被注释数字显示其上全面
落后朴素 RAG）。Pi-Serini 2605.10848 只调 BM25 的 k1/b 两参+扫检索深度，就把 surfaced
证据召回推到 94.7–95.8%——**组织升级的收益必须对着调优稀疏基线验证**；其调参方向明确归因
于长文档域，短 passage 语料只搬程序不搬数值；其 surfaced 94.7→previewed 73.6→behavior
58.9 级联是"检索召回不转化为使用"的独立证据。**记忆支线**（A-MEM 2502.12110/MemRL
2601.03192/Decocted 2604.04373）组织的是系统自产经验，按 §1.3 二分归 R3，此处只借四条
纪律的形状：逐条效用记账、蒸馏修正优于原始记录（revised lessons）、检索相关性提高时任务
效果非单调、对称调参（headline 不得只给自家臂调参）。

### §3.2 视觉域组织演进（D2 源核版）

Wiki-LLaVA 2404.15406 用两层**串行** key（冻结 CLIP 图像向量匹配 wiki 标题、再由
Contriever 以问题检 600 字符 chunk），并自测出两个对本方案关键的读数：带检索反而净负
（26.4 < 无检索微调基线 28.5，且检索条数增加单调劣化），以及**微调的能力代价——
MME-Perception 从 1513.3 跌到 802.8（−47%）**，是"不动参数"红线最有力的跨域证据。
EchoSight 2407.12735 把第一跳换成同模态图像向量 key（击败图像→实体名文本路线：检索 R@1
13.3/36.5 vs 3.3），并**对同一 section 并置视觉面与多模态面两个 key、以全局常数 α=0.5
线性融合**——多面 key 概念在视觉域 2024 年已落地；其 oracle 三级阶梯相对实测的可恢复余量
达 48.1 个百分点。VAM 2605.16481 是 training-free 的在线层次视觉记忆，**已实现跨模态多面
key 与 query 面路由的工作代码**（planner 按 sources⊆{frame,event,summary} 选面）——但
**无融合规则（两面分数的组合从未指定）、零消融，且其 Limitations 自陈缺副语言/音频态面**。
GranuRAG 2605.15019 的"两级证据"实为两份 value 无条件拼接（scene 级 key 只在构造附录、
评测管线未运行），作多粒度意识定位、不作多层 key 证据。MemLens 2605.14906 给出**面特化的
最强实证**：单一词面 BM25 key 在时序检索轴登顶（62.50）而在实体消歧轴垫底（18.03），34
个系统跨类型相关性低——"key 的面决定它能检回什么"有了直接证据；同时其冻结骨干保
abstention（77.27/68.18）vs RL/LoRA 记忆后训练把 abstention 摧毁至 9–22%，是 TFRL 的又一
支持读数。（本段 VAM/MemLens/MementoGUI 属记忆类载体，按 §1.3 二分只作组织/供给形式的
形状借用。）**空位结论**：多面 key+面路由**概念已被占**（EchoSight/VAM）；读集内仍空的是
**音频/副语言面、query 自适应的确定性融合规则、面级消融**三处——语音域 O-config 臂的
增量钉在此三处。

### §3.3 语音域现状

检索线三篇：组织 schema（版本/出处/冲突/引用/abstain）逐字段为零；每 value 一个单向量
单面 key。agentic 线两篇：无持久组织、开放 live web、无快照无落盘（golden_path 仅实体名
链、480/640，trace/* 160 条无该字段——本地实测）。图/层次索引、多面 key、多粒度、面级
消融：语音域读集内全部缺席。

### §3.4 阶段二研究内容：key/value 建模三问 + 统一接口 + TFRL 优化

**问一：key 如何建模**——三子维设计空间：**切片**（单元边界/尺度：定长窗/VAD/说话人轮次/
事件边界/多尺度；VAM 的 Otsu 自适应边界=training-free 在线切片先例；专家模型的窗长需求使
切片按面而异）×**面装载**（哪些信息面进地址空间：语义/副语言/说话人/事件/**时间锚**——
时序不是独立灾难，分解为 key 面/出处字段/动作区间参数）×**表示**（冻结编码器选型；每面
过**可检索性前置检定**——emotion2vec 类非检索目标训练，不过检定的面不入对照）。key 为
多假设式（§1.2 结构面）。

**问二：value 如何建模**——value=检索到的内容如何被使用的建模，形态（标签/句子/语音段/
图）服从使用端合同：冻结核要能解析（omni 核可原生消费音频态 value——对文本核级联的预测
优势）、准入门要能判断（出处+佐证标记）、判据要能审计（时间锚+内容 hash）、**动态层要能
再加工（保留原始可切片材料）**。value 合同一页纸预注册，非实验轴。

**问三：任务端到端优化（TFRL）**——key/value 建模空间的配置（切片策略/面组合/融合权重/
候选宽度 N/**物化边界**）以任务效果为目标做 training-free 优化。**档 A（入合同）**：dev
上 reward-guided 配置搜索，冻结后进 test（ASR 史上 LM 权重/beam 调优=该方法论四十年
先例）；**档 B（扩展）**：运行期按 query/类别自适应配置选择。占位：任务奖励驱动的知识
组织优化在读集内无人做（最近邻 MemRL 的 utility-Q 属记忆维度；AutoML-for-RAG 类占位
补扫义务）。

**统一知识接口与两层绑定**：系统只有一个可查询对象——知识系统对外回答查询、对内自决
**存量 or 现算**（能力池=接口的现算后端：冻结算子默认不跑、按需调用）；**物化边界（哪些
预提取进 key、哪些留按需现算）=早/晚绑定线=TFRL 优化变量**。检索架构=**静态粗召回**
（粗切片+稳定多面 key→候选邻域）+**动态细加工**（查询条件化的再切片/再取面，仅对候选
邻域）；长音频输入超出工作上下文且被反复查询时即"知识化"入 T2 库——双空间在此统一。

**实验重构（O-config 臂族）**：incumbent 配置（单向量平铺定长切，读集现状）vs 任务优化
配置（档 A 搜出）vs random-config（同空间随机采样——优化器须赢过随机搜索）；切片子维先用
检索层读数（K5-r，AR@k 型）廉价筛选；优化配置臂的任何调参须给 incumbent 配置臂等预算
（对称调参纪律）；O-config 臂主锚 SLUE-SQA-5
（Spoken-SQuAD 派生自 SQuAD，DPR 实测调优 BM25 在其上全面反赢稠密 68.8 vs 51.6，语义面
易饱和）；O 臂占实验资源小头预注册。

---

## §4 阶段三：知识如何使用——门控查询-裁决环

### §4.1 文本域供给/使用演进（D2 源核版，压缩）

触发线：Self-RAG 2310.11511（reflection token 训练进词表——原理同形实现有训练；其自身
证据显示纯自决弱于外显阈值 28.3 vs 45.5——**正确原理表述="可校准连续信号+外显阈值"**，
恰为本方案形态）；CRAG 2401.15884（训练的 T5 评估器触发纠正；training-free 替代价格
−19.6；属"总是检索"端）。agentic 深搜按训练态拆：training-free 支=Search-o1 2501.05366
（零训练暂停-取证-插回；不确定词频统计了却没接进决策——判据占据尚空；"无有用信息"整批
拒收门可借）；训练支=Search-R1 2503.09516（"被强化"非"被定价"；其自表中 training-free
复现在 3B Bamboogle 0.320 完胜其 0.088——OOD 脆性；收益能力门控）、DeepResearcher
2504.03160。donor 定价/校准线四篇（借效果论据、定价形态归后期）：CTA 2602.16699 先校准先验再按闭式
规则判取，并示证**"无条件检索是默认失效模式"**；VOI-search 2605.05701 实测中预算档有时
胜过高预算档——花费与效果不单调；PRA 2604.09482 用 margin-shift VoI 免负类标签、打分器
与策略分离防污染；AdaCompute 2604.14853 证明最优预算随类别/实例而异（其 Lagrange 再分配
形态归后期）。使用端：Decocted 2604.04373 证明上下文信息增益可为负（"更相关≠更好"）；
WebThinker 2504.21776 把证据消费组织成 draft/check/edit 的证据态写作。**底盘谱系**：
ReAct 2210.03629（Thought→Action→Observation 环）与 Reflexion 2303.11366（失败后语言化
反思重试——本方案以定向反思统一律约束其适用条件），pre-2024 原理例外。

### §4.2 视觉域门控与协议化（压缩）

ToolGate 2606.03054（pre-call 门；15,782 次标注：11.8/9.9/78.3——获取不免费且多半惰性；
prompt 自我怀疑无效）；FOVEA（贪心可证不足）；MementoGUI 2605.18652（训练态双门先例；
带反事实臂的"无条件检索净负"：随机 64.46<不检索 67.69<学习筛选 68.32）；cotomi
2605.03231（两条 what-changed 通道**部分替代**：合并 +13.8 < 单项之和 +22.1）；M3-VQA
2604.25122
（六协议七臂、gold 三级粒度拆 headroom、全程零训练）；regime 边界 2606.28864（内部算力
对感知任务系统性有害——零音频，仅 H5 类比；其"无条件重表达垫底"是定向反思统一律的
反面教材）。搜索 agent 主线三篇（MMSearch/MMSearch-Plus/OpenSearch-VL）合计零准入/零
显式停止/零归因——**donor 支线成熟、主线与语音域同空白**；MMSearch-Plus 的 under-call
归因（9/10 全程未调）=失效面的另一半。

### §4.3 语音域现状（两线五失效，压缩）

外部知识线（AudioRAG/Omni-DeepSearch）：自由生成+硬编码常数、无条件进上下文、over-search
自证（A.6；预算档非均匀统一封顶——Gemini-3-Pro 口径，两轴分层）。感知工具线（AuTAgent/
EChO/AudioGenie/Agent-Omni/NAP + ARC 2602.14224 横截面〔Agent Track 已公开描述的前三名，
3/16 有偏样本〕）：全部音频重表达、零外部知识动作（Track 1 明文禁搜索引擎）；两条聚合级
负读数（EChO 65.4<68.7；AuTAgent All-Tools 40.90<50.00 且 3/6 工具单独为负=音频域读集内
首个
harmful-acquisition 基率）；prompt 自我怀疑实测无效（Agent-Omni 40.6% 多跑轮 accuracy
变化 0.00）；"新增 LLM 代答吃掉归因"两个干净实例（AudioRAG +9.2 不可归因；AudioGenie
换作答 LLM +9.13/+5.30 vs 换音频模型 3pt 级）。**精确量词**：读集内没有任何一处以
training-free 外显标量门控"外部知识获取"的推理期控制器（NAP 有标量阈值门但训练态且无
知识动作；AuTAgent 差分奖励只在训练期——其量=本方案推理期估计的 delta_E）。

### §4.4 阶段三研究内容：门控查询-裁决环

**底盘继承与门控化**：ReAct 环（动作=对统一知识接口的查询）+ Reflexion 式反思——但按
**定向反思统一律**约束：**反思环有效 iff 注入假设条件化的新证据**（纯语言自省/无条件重
表达/无假设取证三者皆死——Agent-Omni 0.00、2606.28864 垫底、A.6 有害与 NAP/ARC 冠军的
有效重查在此统一）。读集内全部音频 agentic 系统都是自由生成环——**门控化的 agent 环
即空位**。

**双源触发电池（模型零自省，全部外置确定性信号）**：

| 感知列（触发 RE_RESOLVE） | 知识列（触发 SEARCH） |
|---|---|
| 探针实体槽离散度（核 N 采样=其 lattice） | 答案槽探针离散度（无检索态） |
| 前置工具自报置信度（若双通道配置） | 验证式反查失败（候选答案廉价定向验证，gapCorr） |
| 声学质量先验（DSP） | 结构性问题先验（dev 标定的类别基率表） |
| 知识侧验证失败（实体查无此物） | 实体-属性覆盖缺口（问题问音频中不存在的属性） |

对冲："自信幻觉"由**验证优先**兜底（答案承重槽即使探针一致也过一次廉价验证）。触发=三元
决策：**路由**（槽型-面路由表：实体事实→web、发音歧义→发音库、视觉指涉→图搜、音频本体
→T2 库）+**构造**（多假设 query）+**深度**（验证式单查→全量检索→多跳，成本排序级联）。
**触发门质量自身为必报读数**：双混淆矩阵（听错触发对 TTS 最小对 gold；知识触发对先导
数据集负类 gold）——"校准先验判取"这一跨域机制（CTA 一系）的可测性由先导负类首次解锁
（现有载体无负类、不可直测）。

**核的角色（三不+一是）**：不规划、不自省发起、不垄断感知（对齐损失挤掉副语言信息——
**核非全能感知者公理**，项目内部经验+公开测量补扫义务）；**是候选裁决者**：在准入后的
"假设×证据"上裁决与作答——验证易于生成，这是核的强项，能力假设被压到最小且可预检。
RE_RESOLVE 分面路由：语义面→音频段重递核（真重听）；副语言面→专家工具对区间再加工、
符号化输出递核。"第几步重听"=一等设计变量（KV 干预证据显示注入存在有效窗口）。

**结构防爆四机制**：单默认通道（每任务一条最廉价前向）+中控按需调度+**一切查询输出落
候选区、经准入门才见核（总则）**+知识层摊销。**前向通道=harness 实验轴**：{omni 单通道
vs ASR+omni 双通道} 由实验裁决（语义理解准确率），如实报告；蜕壳条件登记。

---

## §5 载体与数据

### §5.1 任务谱系×机制激活矩阵（逐行 delta 符号预注册）

| 任务族 | 分型 | 激活子集 | 主张符号 | 绑定判据 |
|---|---|---|---|---|
| MMAU/MMAR（短片段理解） | 音频自足 | A0 主路径；面工具增强；触发以不触发为正确 | 总分**非劣性**；副语言子类正 delta | 触发特异性混淆矩阵（§6.5 必报）；非劣界预注册 |
| SLU（SLURP 类） | 音频自足+实体槽 | 发音库+个性化（PS-abl/P13N 臂） | 实体重切片正 delta | K-PS；W1 网格现成 |
| **实体/知识密集 ASR（NB 三臂战场）** | 识别+知识 | 发音库+反思修正 | 实体准确率+该集 WER | **K-NB**；GER/biasing 具名对手。**载体=待定向盘点后选定**（候选=biasing/GER 文献标准评测协议〔LibriSpeech 偏置协议、实体标注财报语音集一类〕——盘点在 §9 调研义务内；自建候补=先导数据集实体密集切片）；载体选定前 K-NB 记 `PENDING_CARRIER_SELECTION` 不判 |
| Spoken QA（Spoken-SQuAD/SLUE-SQA-5） | T2 | 阶段二全量（O-config/A1′） | 阶段二主张 | K5 两级 |
| SpeechRAG 形态 | T2 跨模态 | 映射+O-config；omni 消费音频 value 的预测优势 | 阶段一/二主张 | K5-r + A1′ 双读数 |
| Omni-DeepSearch/AudioRAG | T1 | 全机器满负荷 | 主效果主张 | K1a/K1b/K2/K3/K4 |
| **先导数据集（自建，§5.4）** | T1+T2 混合 | 负类触发检定+口音三臂+非词面面 | 触发门查准查全；个性化增益 | 双混淆矩阵 gold；K5 前置㈠ 的合格载体候补 |

闭源 API（Gemini/GPT-4o-audio 级）双角色入对照：裸基线行+**harness 可移植性行**（同一
门控环+知识系统换核即插——system-level 北极星最直接读数）。

### §5.2 现有载体四问（D2 版结论，压缩）

**Omni-DeepSearch-640**：构造纪律音频域内最严（GPT-5 过滤+KG k≥5+人工复核；过滤模型
远强于评测核=有利不对称），但跨域五轴无一领先（快照/split/judge/过滤器纪律/去污——
MMSearch/MMSearch-Plus/OpenSearch-VL 分别持最优）；仅 train split、无快照、无 no-tool
行、15 类每类约 43 题撑不起 12 模型排名；本核 6.56 近地板→**方向性主载体**。
**AudioRAG-500**：过滤器与判官见 gold、公开可得性未确认；次载体（K3）。
**VoiceAgentRAG-200**：放弃（仅 R9 参考）。**锚数据集**（Spoken-SQuAD/SLUE-SQA-5）：
gold passage 官方存在；TTS/朗读态作诊断锚；O 臂主锚=SLUE-SQA-5。

### §5.3 载体风险与回退梯（件内自足）

本核近地板风险（主载体 6.56；弱模型 gold-entity 也仅 22.03，作者原句支撑数对
12.50/11.72；correct→wrong 基数约 40 题级）+"能力缺失 vs 调度缺失"判别（NAP 同族前代核
〔Qwen2.5-Omni-7B，跨核跨代仅形态类比〕时序定位近地板经训练升 6–10 倍；本核 MMAR
68.70/69.00〔EChO 报告值〕说明落差在控制面非感知）→ 灵敏度前置+判别力条款（§7）。
**回退梯**：①按官方 pipeline 校核本核配置后重测灵敏度；②分层收缩到本核有牵引力的类别
子集（本核口径最高 IMAGE 10.62/SINGLE 9.38，SPEECH 5.83 反为最差层之一——他核消融的 SPEECH
强势不得串用），主张同步收窄；③主载体降级为方向性证据、AudioRAG-500 升主（raw 37.0 远
离地板；其 gold 泄漏污染面随升格进入主结论限定，信息角色边界声明按其构造重述）；④梯尽
则不得宣布任何判死或胜出、回 owner。O 臂锚（SLUE-SQA-5/Spoken-SQuAD）失敏同用梯④出口。
judge-API 依赖与 split 冻结属执行合同问题，登记不解决。

### §5.4 先导数据集构造（门二的解，两层设计）

**主载体层（真实长音频改造，先导 150–200 题）**：源=播客/会议/讲座/长访谈（GraphRAG 的
Podcast 语料先例）；问题按三类信息需求设计（定位稀疏片段〔时间戳 gold〕/非词面面承重
〔情感/说话人/事件为 key〕/音频实体→外部世界知识）；**负类实例按构造标注**（信息角色
gold——"该学会不搜"在读集内首次有载体）；外部语料冻结快照、dev/test、no-tool 地板行+
oracle 行、
第三方冻结过滤器不见 gold（D2 五轴规格书）；**口音/说话人分层轴**（标准/带口音/重口音
三档——个性化三臂检定：仅标准库 vs +变体族 vs +克隆 key）。**诊断层（TTS 受控合成）**：
听错最小对（Meyer/Mayer 按需生成）、WER 分层扰动、A1′ 泄漏掩码材料——TTS-ASR 近逆性
使其不可作主载体、恰可作受控诊断。全量构造过"大工件三问"后另批。**自建基准+自评方法的
COI 对策**：基准独立发布、方法评测预注册。

---

## §6 实验设计与方法合同

### §6.1 门一：能力预检电池（Stage-2A 第零步，约 50 题级；W1 网格资产裁子集，①③④ 需
按面/按裁决形态补构造少量题目）

五项预检，**每项挂 pass/fail 路由（K-Gate）**：
①分面感知普查→丢失面清单→RE_RESOLVE 面路由表（清单为空的面：对应联邦组件登记蜕壳条件
并作摘除对照）；
②固定 schema 格式遵从与探针应答稳定性→**不过阈：V̂ 中探针系信号（maxAgree/disp/audCons）
降权至禁用，触发电池退化到无探针子集（知识侧验证+先验表+声学质量），A4b 主张记
`PENDING_CAPABILITY`（K1b 不判）**；
③候选裁决能力（给定 N 假设+证据判优）→**不过阈：'omni 裁决核'角色公理不成立，回 owner
重议核选择（换核/闭源 API），架构主张暂停**；
④核裸语义地板 vs 专用 ASR 的实体差距→超阈：K-NB 主张降级为理解任务限定；
⑤前向通道读数（omni 单 vs ASR+omni 双通道语义准确率）→FC 配置裁决输入。
候选核={开源 omni（Qwen3-Omni 系）、闭源 API}——核的选择是读数不是假设。

### §6.2 臂族（重构版因果阶梯）

| 臂 | 阶段 | 识别对象 |
|---|---|---|
| A0 核直答 | 卫生 | incumbent 地板行 |
| **NB 三臂（新基线主张）**：omni 裸核 / ASR+biasing-GER / omni+知识系统（**主张臂运行时 ASR-free**） | 一+三 | 范式替代主张（实体/知识密集集；K-NB） |
| **PS-abl 发音库消融**：主张臂 − 实体发音库（其余不变） | **一（机制核归因）** | 发音库的独立贡献（K-PS） |
| **P13N 个性化三臂**：仅标准库 / +口音变体族 / +克隆 key（口音分层轴上） | 一 | 个性化机制逐级贡献（K-PS 域） |
| FC 前向通道消融：{omni 单, ASR+omni 双}（**不适用于 NB 行**；若在 NB 战场双通道胜出，范式主张按 K-NB 降级条款改判"增强"） | harness | 前向配置裁决+蜕壳条件 |
| A1 gold-entity ceiling / A1′ 双读数（naive/masked+泄漏占比） | 一 | 映射/使用损失分离 |
| O-config：incumbent 配置（单面单向量 key，读集现状形态）/ 档 A 优化配置 / random-config | 二 | 组织优化的贡献（含切片 K5-r 预筛） |
| A2 检索+无条件拼接 / A3 同证据集+准入 | 三 | 管线总收益 vs 使用机制独立贡献 |
| A4a 通用调度（V̂_gen 无感知输入；探针两臂共用仪器）/ A4b=+感知路由 | 三 | **双源机制唯一识别：主张挂 A4b−A4a**（K1b 合取+配对 COST_DISPARATE） |
| A5 证据扰动（无 LLM 生成器） | 三 | 盲从/污染/correct→wrong |
| A6 已执行池离线 oracle | 三 | recoverable headroom（A1 缺位替补） |
| VF-abl 验证优先消融：{开, 关} | 三 | 验证优先条款的净贡献（与"无条件获取=失效模式"的调和=它是**槽条件化**的定向验证非无条件获取，消融判定其值） |
| H 行：harness 可移植性（换核即插） | 系统 | system-level 北极星读数 |
| 蜕壳检验：预检超阈组件的摘除对照 | harness | model-eats-harness 学说的可验证化 |

对照臂纪律（件内全列）：固定档阶梯扫至成本不低于 max(A4a,A4b)；random-matched-cost
（不显著低于 A4a 则 K1a 读数标 ALLOCATION_UNPROVEN）；re-resolve-count-matched；
always/never 角点；ASR-dump 与 EChO-dump（同工具无条件直灌——选择性路由的归因对照）。

### §6.3 方法合同（五元组，统一接口版）

```text
state_t   = { H_t 多假设槽集（N-best 式，含 agree(h)）, E_t 已准入证据（corr(e,h)、出处、
              时间锚）, b_t 分维记账余额, 动作历史 A_{1..t-1} 与候选答案漂移 }
action_t  ∈ 对统一知识接口的查询族：{ RE_RESOLVE(区间,面), RE_SLICE(邻域,粒度,面),
              SEARCH(q,库), ADMIT/REJECT(e), ANSWER, STOP/ABSTAIN }
V̂ 估计量族（信号分层=A4a/A4b 差分的定义所在）：
  A4a: V̂_gen(SEARCH(q)|s_t)=α2·gapCorr(E_t,ĥ_t)+α3·nov(q|A_{1..t-1})   ←无感知不确定性输入
  A4b: V̂_aud=V̂_gen+α1·(1−maxAgree(H_t))；V̂(RE_RESOLVE|s_t)=β1·disp(H_t)+β2·(1−audCons(ĥ_t))
  准入: V̂(ADMIT(e)|s_t)=γ1·corr(e,ĥ_t)+γ2·srcQual(e)−γ3·confl(e,E_t)
  再切片: V̂(RE_SLICE(邻域,粒度,面)|s_t)=δ1·gapCorr(E_t,ĥ_t)+δ2·(1−menuCov(邻域))
    ——menuCov=该候选邻域内已尝试的（粒度,面）组合占预注册再加工菜单的比例（确定性计数；
    菜单=authorization 预注册）；δ 权重与 α/β/γ 同批 dev 标定。§6.4 所称"确定性策略"即
    本式+policy 阈值规则，无第二套机制。
  可实现定义：maxAgree=采样候选答案语义槽众数占比；disp=1−maxAgree；audCons=已执行重呈现
  中首选假设复现比（无重呈现=1）；corr(e,h)=槽值重叠（实体槽=字符串/别名匹配、类别槽=
  类别及别名表匹配）；gapCorr=1−max_{e∈E_t}corr(e,ĥ_t)（只按首选假设计，不从证据侧反推
  假设全集后验）；nov=与历史 query 最大重叠之补（扩展为（动作类型,时间区间）对）；srcQual
  =预注册白名单查表；confl=同一语义槽取值冲突计数。时序合法性：全部输入仅依赖 t 步及以前
  观测；实际回报仅离线标定不进在线决策。探针仪器声明：逐步候选答案探针为 A4a/A4b 两臂
  共用仪器（同频同额计费，A4a 的 V̂_gen 不读取），差分中探针成本抵消。触发电池（§4.4
  两列）为估计量族的传感器层——本版已接入 disp/maxAgree/gapCorr 三路；**其余五路信号
  （工具自报置信度/声学质量先验/实体存在性验证/类别先验表/属性覆盖缺口）的估计量接入
  形式与权重列入 §9 义务**，接入前它们只作门控预筛的独立阈值信号、不进 V̂。
policy    : argmax_a V̂(a|s_t)；max_a V̂ ≤ τ 时 ANSWER/STOP；τ/α/β/γ 与融合权重、候选宽度
  N、切片配置=TFRL 档 A dev 标定（禁 test gold）；硬上限=工程护栏只记录。
transition: 一切查询输出先落候选区，仅 ADMIT 进核上下文；RE_RESOLVE/RE_SLICE 更新假设与
  邻域；一切写入带出处与时间锚。
```

### §6.4 复放与污染审计

pin 服务/日期/参数；逐次落盘 URL/文档 ID/rank/内容 hash；共享查询跨臂复用；**动态层确定
性**：再切片/再取面由确定性策略驱动且全程 trace（否则复现性在动态层重新断裂——硬约束）；
可达性/污染分层单列。可选请求（owner 未裁不生效）：检索正文单向落盘诊断槽。

### §6.5 四层评价（不合成总分；件内自足）

- **有效性**：反事实边际效用 delta_E=U(M(x,q,E),y)−U(M(x,q),y)；official accuracy、
  paired delta、bootstrap 95% CI、McNemar、SESOI、wrong→correct/correct→wrong、按任务
  类别/音频类别/hop 深度/信息角色（可判定载体）分桶。
- **合理性**（离线诊断）：retrieve-skip、continue-stop、admit-reject 混淆矩阵；**触发门
  双混淆矩阵必报**（听错触发对 TTS 最小对 gold；知识触发对先导负类 gold）；V̂ 各分量对
  离线 delta_E 的标定与误差界（AuTAgent 差分奖励=同量的训练期先例）；answer-bearing
  coverage、出处覆盖、unsupported claim；**under-call 读数**（A6 池内存在能翻正动作而
  未执行的占比）与 **no-op 检索检出**（重复 query/固定文档回流——RAG 实测的伪装形态）；
  removal/swap 反事实。
- **可靠性**：seed/run 方差、correct→wrong、worst-group/尾部、coverage-quality、跨音频
  类型与口音分层的符号一致性；abstain 不得靠压 coverage 造安全。
- **效率（记账不设限）**：九维成本向量（retrieval hops, result bytes, core calls, audio
  seconds, controller tokens, judge calls, wall-clock, API currency, index/snapshot
  amortized），均值+P95；探针=两臂共用仪器同频同额计费；含/不含探针双读数；**每实体修正
  边际成本单列**（核当语义通道比 ASR 贵一至二个量级，如实暴露）；等成本/等质量/Pareto=
  后期诊断不进主判据。

---

## §7 击杀判据（定义=本版件内自足；数值=authorization 前 power analysis 预注册）

**灵敏度前置检定**：任何判死只在载体过检定后有效——①A1（trace/* 不可构造子集由 A6 替补）
−A0 ≥ 预注册最小可检余量；②固定档曲线斜率非零；③correct→wrong 样本基数 ≥ 下限。
**判别力条款**：A1 与 A0 皆近地板且差值低于余量=核能力缺失（gold 假设都救不动），不判
杠杆死、直接走 §5.3 回退梯——"杠杆死/载体无分辨力/核能力缺失"三分。

- **K1a（通用调度）**：A4a vs 效果最强固定策略（阶梯扫至成本不低于 max(A4a,A4b)，取效果
  最高档×选定 re-resolve 档；并列论文 SOTA 臂与 random-matched-cost），paired delta 95%
  下置信界 ≤0 → 调度类杠杆判死。matched-cost 不显著低于 A4a → 读数标 `ALLOCATION_
  UNPROVEN`；自适应臂成本超阶梯预注册倍数 → `COST_DISPARATE`（仅在阶梯无法扩至成本可比
  时非空转）。
- **K1b（双源独立性=合取条件）**：①A4b−A4a paired delta 95% 下置信界 >0 ∧ ②标定后 β
  权重与零可区分；任一不满足 → 音频特有主张判死、按 §8 路由 MERGE。配对成本可比：探针
  两臂共用仪器抵消；A4b 成本超 A4a 预注册倍数（core calls/retrieval hops/audio seconds
  三维）→ `COST_DISPARATE`，不得单独支撑独立主张。
- **K-NB（新基线判据；统计口径与 K1a 同型）**：NB 战场载体上，主张臂（运行时 ASR-free）
  vs incumbent（ASR+biasing-GER）的实体准确率与任务效用 paired delta 95% 下置信界 ≤0 →
  范式替代主张判死，降级"理解任务限定的知识增强"；若 FC 双通道在该战场胜过 ASR-free
  主张臂 → 同样降级为"增强"并如实改判（替代主张只能由 ASR-free 臂承载）。载体选定前记
  `PENDING_CARRIER_SELECTION`（§5.1）。
- **K-PS（阶段一机制核判据）**：PS-abl 消融显示发音库贡献在困难实体切片上的 paired delta
  95% 下置信界 ≤0 → 发音库降级为工程组件（不承载机制核主张）；P13N 三臂在口音分层上逐级
  无增益 → 对应个性化机制（变体族/克隆 key）逐个降级。降级不判方向死——机制核主张回落到
  双源调度与组织优化两支柱。
- **K2（over/under 双向）**：over-search 事件=最终答错 ∧ t>t*（首个当步候选正确步）存在
  SEARCH；判定输入=已落盘答案与 rank/hash 轨迹；判官抽样复核（抽样框=候选 episode、输入
  不含检索正文、CI 对象=事件率、抽样率与协议预注册）；如实标注为事件率估计非内容级全量。
  A4b 未相对最优固定档降低该率（非劣界预注册）→ 调度杠杆判死。under-call 对偶读数（§6.5）
  同报，不单独判死。
- **K3（复制）**：次载体上按预注册 replication criterion 判方向一致（无分辨力的轻微负值
  不算翻转）。
- **K4（准入门；载体形态前置）**：A3 未降 Knowledge-error 或 type-D 增多 → 判死留 R5/R8。
  前置：准入价值集中于长文/多 claim 任务（Self-RAG：ASQA −14.0 vs 短答 −2.9），短答载体
  上仅当 correct→wrong 基数过检定③ 且证据密集子桶一并为负才生效，否则记
  `PENDING_CARRIER_FORM`。
- **K5（组织优化两级）**：前置㈠锚上非语义槽 query 占比 ≥ 下限、㈡incumbent 配置
  （单面单向量 key）与调优稀疏基线的
  检索层读数距天花板有余量（不过则不判死、回 owner）。K5-r=检索层报告读数（AR@k，含调优
  稀疏与深度对等，按面分层，不携判死）；K5-t=档 A 优化配置相对 incumbent 与 random-
  config 无任务效用/coverage 增益 → 组织优化不立主张、回落实验合同角色（判死权仅在
  K5-t，且仅在具备非语义槽需求的载体上生效，否则 `PENDING_CARRIER`）。
- **K-Gate（门一判据；五项预检全部挂路由，§6.1）**：②探针稳定性不过阈 → 探针系信号禁用、
  A4b 记 PENDING_CAPABILITY；③候选裁决不过阈 → 裁决核公理不成立、回 owner 重议核；④核裸
  语义差距超阈 → K-NB 降级理解任务限定；①丢失面清单为空的面 → 组件登记蜕壳条件并作摘除
  对照。
- **非劣性读数（MMAU/SLU 行）**：非劣界预注册，READOUT_ONLY——不携判死权，只作主张矩阵
  的符号验证。
- 多杠杆×多集×多分桶按预注册 multiplicity correction（Holm/max-T，复用 W1 统计基建）。

## §8 管辖界线与独立性

R2=语音任务中**知识**（世界写的）的系统研究；R3=记忆（系统自产 revised lessons——个人
发音档案为其第一用例）；R6 消费 action 定义；R8 消费可靠性阈值。接口：R2 产 episode
原料、只消费冻结预注册先验（含工具可靠性表），在线更新归 R3。

**方法位定位表（读集内，cut 2026-07-30）**：

| 系统 | 训练态 | 控制信号 | 动作源 | 作答权 | 知识组织 |
|---|---|---|---|---|---|
| AudioGenie | training-free | 自由生成二元 flag | 单源重表达 | 新增 LLM 代答 | 无 |
| NAP | SFT+RL | 标量阈值 | 单源媒体重取 | 冻结核 | 无 |
| ARC 冠军 | 未声明 | 未公开 | 单源工具 | 未声明 | 无 |
| AuTAgent | RL 工具选择 | 训练期差分奖励 | 单源工具 | 冻结核 | 无 |
| AudioRAG/Omni-DeepSearch | training-free | 自由生成 | 含外部知识 | 代答/冻结核 | 无（live web） |
| contextual biasing / GER 线（**占位：未读，§9 补扫义务**） | 训练进解码器/纠错器 | — | 词层候选 | — | 每句人工候选表 |
| **R2 本提案** | **training-free** | **可标定标量+外显阈值** | **双源同尺度（重听+知识）** | **冻结核** | **发音库+T2 库+面联邦（TFRL 优化）** |

组合格读集内为空（**未读项如实列**：speech-hands 2601.09413〔一等 fetch〕与
biasing/GER/lattice-STD/检索增强 ASR 四支〔占位补扫，表内该行即占位刻画〕——落笔前补扫，
结论带读集量词自限）。
prior-difference：训练态门先例（MementoGUI/NAP）→独立性挂三条合取非"有没有门"；trained-
comparator 反驳落红线+信息边界（AuTAgent weak-to-strong 拆"训练太贵"；其自证决胜是决策
目标非训练——差分奖励 +2.80 vs 二元 +0.20，跨表拼接口径可对接）。**长期定位（harness
学说）**：感知 harness 会被吃（蜕壳条件已登记）、门控是核当前能力的探针、**知识层结构上
吃不掉=方向护城河**。

| MERGE 触发（任一成立即路由 R6/R8） | 判定载体 |
|---|---|
| 唯一新内容=通用 query/hop/stop | K1b 不成立而 K1a 显著 |
| 信号/状态/阈值与其他 action family 完全同形 | V̂ 的 β 分量标定权重归零 |
| 音频只是输入载体 | A4b−A4a 在全部音频类别分桶无一显著 |
| 无法独立消融 | A3/A4 析因执行期不可实施（如实报告即触发） |

## §9 边界、证据底座、义务与处置

**红线与合规**：§1.4；API-only；test gold 永不进 controller；H5 withheld。

**证据底座**（全部承重引用回溯 `wiki/survey/2026-07-17-sf-fulltext-ledger.jsonl`；逐 id
枚举件内自足）：
- **深读层 A（dossier，17 篇）**：AudioRAG 2602.10656、Omni-DeepSearch 2605.08762、
  VoiceAgentRAG 2603.02206；SpeechDPR 2401.13463、SpeechRAG 2412.16500、WavRAG
  2502.14727；donor：ToolGate 2606.03054、FOVEA 2605.01345、CTA 2602.16699、VOI-search
  2605.05701、PRA 2604.09482、Decocted 2604.04373、MemRL 2601.03192、AdaCompute
  2604.14853、WebThinker 2504.21776、**ReAct 2210.03629、Reflexion 2303.11366**（本版
  回归，反思环原理锚，pre-2024 原理例外）。
- **深读层 B（D2 条目 33 篇，d2-entries/ 逐篇含源核；篇名-id 对照使正文裸名件内可解析）**：
  文本线=CRAG 2401.15884、GraphRAG 2404.16130、HippoRAG 2405.14831、LightRAG 2410.05779、
  Search-o1 2501.05366、Search-R1 2503.09516、DeepResearcher 2504.03160、A-MEM
  2502.12110、Pi-Serini 2605.10848；视觉线=Wiki-LLaVA 2404.15406、EchoSight 2407.12735、
  M3-VQA 2604.25122、MMSearch 2409.12959、MMSearch-Plus 2508.21475、OpenSearch-VL
  2605.05185、GranuRAG 2605.15019、VAM 2605.16481、MemLens 2605.14906、cotomi
  2605.03231、MementoGUI 2605.18652、TTS-regime 边界 2606.28864；pre-2024 原理/数据集
  例外=RAG 2005.11401、DPR 2004.04906、Self-RAG 2310.11511、ROME 2202.05262、OK-VQA
  1906.00067、A-OKVQA 2206.01718；
  音频域边界=AuTAgent 2602.13685、EChO-Agent 2606.15141、Agent-Omni 2511.02834、
  NAP/OmniAgent 2606.19341、ARC 总览 2602.14224、AudioGenie-Reasoner 2509.16971。
- 同批抓取其余 pre-2024 仅存档不引用；2605.13277 在册未引用不入枚举。（"DeepAgents"在
  正文为工程模式名而非论文引用，具体文献=§9 调研义务"语音域 DeepAgents 应用"。）**新增调研义务（占位补扫，
写"空位"前必做）**：contextual biasing/GER 线/lattice STD/检索增强 ASR；audio-LLM 副
语言损失公开测量；长音频基准盘点；语音语义切分与 chunking 策略；晚绑定检索（late
chunking/RAPTOR/small-to-big）；reward-driven 组织优化（AutoML-for-RAG 类）；音频接地
GER；语音域 DeepAgents 应用；ASR 置信度估计（经典线）；克隆音作 key 占位；speech-hands
2601.09413（一等）；CoFi-Agent 技术报告（二等）；M3-Agent。情感槽外部知识载体调研继续
（MELD-Emotion 已排除：答案=情绪标签本身，落信息角色边界外）——先导数据集的非词面面问题
设计部分承接此缺口。

**authorization 前义务清单（件内全列）**：K1a/K1b/K-NB/K2/K3/K4/K5/K-Gate 数值（power
analysis，含 K5 两条前置下限/余量、两处 COST_DISPARATE 倍数、判别力条款近地板阈值、K4
证据密集子桶定义）；V̂ 估计量族选型与权重/阈值标定协议（含重听窗口参数与探针频率）；
judge 保真合同（含 K2 抽样率与判官协议）；数据集 lock 与分层切分；检索服务 pin；三篇
检索线检查点发布状态核查；面联邦冻结编码器选型与检查点核查（emotion2vec/说话人/事件系
的发布状态/版本/许可）及面路由/融合规则与每面可检索性检定协议；各类别型槽分类体系与
别名表（来源/版本/冻结方式）；实例级信息角色标注协议（引入负类载体/受控注入时）；A1′
答案掩码协议（泄漏占比口径）；re-resolve-count-matched 匹配规则；incumbent/优化配置
两臂检索深度对等
口径；探针成本记账口径；A5 确定性种子协议；O 臂资源占比与构建开销口径；**新增**：能力
预检电池题集与阈值；NB 三臂集合与指标口径；FC 双通道配置；蜕壳条件逐组件登记；先导
数据集构造合同（源许可/标注协议/过滤器选型/快照口径；**先导层与全量层各自过大工件三问
——先导的三答：stage=开题验证、参数=构造合同预注册、子集=150–200 即子集**）；实体发音库
构造合同（实体清单来源/G2P 与 TTS 选型/变体族口音清单/库规模与更新口径）；TTS 与音色克隆
检查点选型（含许可与伦理边界，§2.4 条款）；蜕壳条件逐组件的阈值 X 定义；触发电池其余五路
信号的估计量接入形式与权重（§6.3）；档 A 优化器选型与搜索预算；NB 战场载体盘点与选定
（§5.1 行内候选）；speech-hands 2601.09413 fetch+D2（一等）。

**累计 exposure（件内自足）**：文献 fetch 与全文/PDF/LaTeX 源阅读发生（31 篇 known-ID
抓取 62 行 ledger 全 200；5 次 web 检索〔3 次题名→id 解析+2 次 CoFi-Agent 解析〕；1 次
fetch 调用返回 SKIP 已在册；33 篇 D2 深读的本地 eprint 解包与 PDF 抽取〔会话 scratchpad〕；
round-04..09 评审的本地资产读取）。零模型/API 执行、零指标运行、零数据集下载、零复现、
零原型；v11 为纯文本合成（十六轮讨论全程零执行，裁决见 Decision-Log 续81）。

**评审史与本版性质**：v3–v10 经 round-03..09 七轮评审收敛；v11 为十六轮对抗讨论（续81）
后的结构性重构，经 round-10 全量重审判 6 MAJOR+16 MINOR（续81 十六条零遗漏零反向变形、
round-09 十三项修补 13/13 未回流、数字改写为零——皆 round-10 核证）。**本版（v12）逐条
关闭 round-10 全部发现**：机制核补消融臂（PS-abl/P13N）与判据（K-PS）；RE_SLICE 补 V̂
估计量（menuCov 式）；O1/O2/M3 遗留符号清零（自足性恢复）；NB 战场载体如实改
PENDING_CARRIER_SELECTION 并列盘点候选；主张臂钉 ASR-free+FC 降级改判条款（续81⑮⑯
接缝闭合）；门一五项预检全部挂 pass/fail 路由；§8 占位行标注+保留句扩至四支未读近邻；
cotomi 恢复 22.1 支撑数；层 B 枚举改篇名-id 对照；四处量词补 scope（含"最差层之一"
恢复）；§3.1/§3.2/§4.1 三节按可读性要求恢复指标名与说理散文；验证优先加 VF-abl 消融
开关；克隆伦理边界入 §2.4 与义务清单；蜕壳阈值/发音库构造合同/五路信号接入/NB 载体盘点
入义务清单；大工件三问覆盖先导层。数字纪律：v12 未改动任何已源核数字（恢复的 22.1 出自
cotomi D2 条目源核）。

**处置建议**：`GO_STANDALONE_AS_SPEECH_KNOWLEDGE_SYSTEM`（知识主题/agent 底盘/语音
战场；新基线三臂主张+双源机制识别+组织 TFRL 优化三支柱）。**最强反方**：①"这是 ASR
rescoring 换皮"→反还原不变量+NB 臂正面交锋+§8 定位表；②"组合格空位未经近邻补扫"→
占位义务清单已立，落笔前补扫，量词自限读集；③"载体自建自评 COI"→基准独立发布+预注册；
④"trained 路线效果更高"→红线+信息边界+AuTAgent 自证；⑤"model eats harness 吃掉方向"
→可吃性分层，护城河在知识层。

**owner 裁定栏**：＿＿＿＿（结论/日期/Decision-Log 条目号）——续77 有条件 GO 已录、
续79/80/81 系列设计裁定已录；v11 收敛版的生效落笔权在 owner。
