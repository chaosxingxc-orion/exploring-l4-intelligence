---
title: "D3 — 5-persona strict review of the omni-agentic survey paper (verdict + must-fixes + resolution)"
date: 2026-07-06
stage: 1-argumentation
role: review-record
---

# D3 review record

Five independent adversarial personas (EIC, methodology, domain-expert, cross-disciplinary,
devil's-advocate; Opus, blind) reviewed `2026-07-06-omni-agentic-systems-survey.md`. **All five: `sound-
with-corrections`.** Editor: *the map (four-family taxonomy, ~120-system ledger, sensor-vs-brain landscape,
the perception/planning/verification/real-time reclassification, the localized voice-agent empty cell) is
real and Stage-1-appropriate; citations resolve clean; **but the central thesis as sold must be re-scoped
before the owner selects a problem.** None of the four blockers needs new experiments — all are re-statement
and honest caveating.*

## Must-fix (all applied in paper v2 + synthesis + framework)
1. **Thesis scoping / family-③ contract reconciliation (all 5).** "Boundary crossed only by a new-info
   element" survives only by excluding family ③ (decoding edits) — but the canonical Project-Thesis
   **explicitly lists "decoding" and "reward-guided decoding" as IN-SCOPE** training-free levers, so
   contrastive decoding is an in-scope info-free ceiling-raiser that was *defined out, not confronted*. →
   **Restated CONTRACT-RELATIVE**: for SELECTION over a fixed generative law with no new external info,
   accuracy is oracle@N-bounded (definitional); raising the ceiling needs EITHER a new-info **element** OR a
   **generative-law change** (③ decoding). ②/③ line redrawn on "selection over a fixed law vs a law-change";
   contract-compliance assessed **per edit** (contrastive/logit-surgery decoding **in-contract** → new lever
   GAP-7; EGLR layer-recursion **out**). **Operational element test** added ("introduces conditioning bits
   not in the model's inference-time input") and applied uniformly (fixes RegionFocus-element vs EGLR-excluded
   self-sealing). Decoding lever surfaced in the owner menu (GAP-7).
2. **Abstract/conclusion over-claim (4–5).** The near-tautology (conceded in §11) was sold as an empirical
   discovery. → Moved the concession into the abstract; contribution restated as the **precise scoping +
   ~120-system classification**, not a surprising negative; claim A ("elements are the only new-info
   carriers" — holds) separated from claim B ("only elements raise the ceiling" — **false**, ③ does it
   info-free); empty cell relabeled "absence-of-evidence under element-oriented querying."
3. **2505.24347 mischaracterization + internal contradiction (2 verified, 1 dissent).** §4/§8.2 said it
   "dissolved into an element (external GPT-4o)"; two reviewers verified a **single-frozen-GPT-4o** 3-stage
   self-check, contradicting ledger line 115 ("no second model"). → Corrected to **UNRESOLVED / parked as
   GAP-6** (real gains vs a naive baseline, **no oracle@N control**), not "dissolved"; "zero survive
   scrutiny" → **"zero CONFIRMED; the strongest is unresolved for lack of an oracle control."**
4. **p6 perception-delta over-claim (2 must-fix).** "Validates omni-as-perception-element / significant" over-
   reads an **own-lossy-Chinese-ASR** baseline; the pattern (largest on SQuAD lexical, **zero on
   paralinguistic VocalBench**) is the **opposite** of what a perception element predicts and favors the
   confound. → Downgraded to "directionally consistent with, **not sufficient for**"; strong-external-ASR
   control made a **precondition**; "significant/validates" dropped as settling words (directional n=60).

## Major (applied)
5. **Lean = definition, not theory-track result.** `readout_acc_le_oracle` is a static identity; per CLAUDE.md
   ("a static identity is not a result"; needs correctness AND convergence) it is not a theory-track result.
   → Reframed as *formalizing the definitional structure of family ②*, correctness-only; the missing dual-bar
   piece = a **convergence** result for the constrained selection process → **added** `BestOfNConvergence.lean`
   (`bestofn_regret_tendsto_zero`).
6. **Oracle is compute-relative.** T5's 42.7% is **oracle@8 at N=8 sampling, not the permanent floor**; "the
   gap is knowledge not decoding" softened to "not addressable by prompt-space edits at fixed N=8."
7. **GAP-1 framing.** Empty cell is unsearched/cost-gated + a partly known-answer demo of W1's lever. →
   Relabeled absence-of-evidence; expected outcome + falsification condition stated; **GAP-3/GAP-6 promoted**
   alongside GAP-1; GAP-7 (in-scope decoding) added.
8. **§7 header.** "competitive-to-superior" is element-quality-confounded (stronger brain vs weaker omni on a
   GPT-judged bench) → header qualified; matched comparisons preferred.

## Minor (applied): "provably"→"demonstrated" (EGLR/Freeze-Omni); 2503.23395 evaluates a self-collected DB
NOT MMAU; "martingale proof"→"empirically, debate ≤ majority-voting" (2605.00914 is empirical); refs add
2510.20867 + 2604.15383; τ-Voice retention quoted 31–51% clean / 26–38% noisy; "3 verification passes"
renamed (existence+topic for 21 IDs, not per-claim); in-house verbs → "directionally consistent with".

## Consensus strengths (kept)
Four-family taxonomy + the perception/planning/verification/real-time reclassification is the real
contribution; the self-aware ~120-system ledger; clean citation resolution; correct domain classifications;
the sensor-vs-brain landscape + localized empty cell are Stage-1-appropriate.
