---
title: "Research Objective & Current State — 唯一热层入口"
role: "现状/对象/约束/open items 的单一默认加载面（工作层,supersede-in-place）。审计真理在 Decision-Log（冷,grep 单条续NN）;被取代内容不在本文件——见 archive/research-objective-supersession-index.md（墓碑）。"
last_refresh: "2026-07-15（续46:S0 草案落盘 + 正典按 owner 裁决重写;S0 签署 = 效力正式化）"
---

# Research Objective & Current State

> 开工只读本文件 + `CLAUDE.md`,北极星再读 [[Project-Thesis]]。记录规约见 [[AI-Collaboration]]。

## 1. 研究纲领（目的链的根）

**Primary program**：能否用一个外部 reward-guided 控制平面（agentic system），把冻结**黑盒**
omni foundation model 组织成能感知多模态环境、维护状态、调用工具、利用反馈并选择下一步动作的
系统，从而把预训练潜藏能力**激活到尽可能高**。——owner 裁决 2026-07-14/15（续45/46）；
**效力正式化待 Gate S0 签字**（[[2026-07-15-s0-program-identity-signoff]]，含 TF-Strict/TF-Core
待勾选项）。**身份层不立法指标**（owner 07-15）：具体指标绑定任务×数据集、在各研究协议中定义
与冻结；度量纪律以硬约束形式见 §3。资源姿态 = **全力摸高阶段**（预算不设 cap、照实记录；
等预算判据 = PHASE-3_TOOL）。

## 2. 阶段与门（现在在哪、下一步是什么）

- **Stage-1A 收尾**；Stage-1B 未放行（流程定义见 [[Research-Methodology]]）。
- **下一解锁动作 = owner 签 S0** → system-first proposal 重写（v2 评审 §11 次序）→ system-first
  survey（v2 §4 八条 agentic lanes；selector round-2 协议降为子包 SURVEY-B——其 21 lanes/105
  预注册查询维持**零执行**、待 search-design 签署）→ Stage-1A close 与 1B 放行 = 两个签字。
- **1B 探针序未对齐待 owner**：续40 已签 P-α/β/γ/δ vs v2 评审代拟 B0–B5——P-α（头空）仍是最
  便宜首验：无头空则任何 controller 无物可兑现。C1/C4 诚信普查 = CENSUS_COMPLETE（续40 批次A，
  owner 于 1B-0 签批时终验；config-selection 轨迹 = 永久缺口，1B 起由尝试登记前瞻关闭）。
- **两轮博导评审均 RETURN_FOR_MAJOR_REVISION**：v1 五承重 P0（记录范围失实、配置化基座、prior
  遗忘、身份收口、数据暴露）继续有效；v2 判主问题错位（P0-SYS-1..7，要求五份系统级合同）。
  现行有效回应 = RESP-02；诚信裁决 = FFP NOT ESTABLISHED / **MATERIAL QRP ESTABLISHED**（更正
  义务在案）。
- 知识栈选型 PARKED（续37，四门复活）；GLAP 全量构建 PARKED 31000/57638（C4 台账在案）。

## 3. 硬约束（全程绑定）

- **核心模型权重与内部架构冻结；外部系统结构显式设计并版本化。** 严格黑盒 headline；本地模型
  logprob = GRAY_BOX_DIAGNOSTIC（校验用，永不承重）。
- **信息边界**：test-item gold 不入 selector/reward/prompt/检索/候选构造（read-out 允许、
  new-info 禁止）；历史暴露按 INHERITED_PRIOR_EXPOSURE 记账，1B manifest 须排除 exposure union。
- 证据全 directional-only / hypothesis-grade，无确证宣称；headroom 归因纪律；ρ 族 cellwise-only
  四量并列。
- 审计层 append-only + 哈希正典 git blob；工作层 supersede-in-place；发布件过敌意内审环。

## 4. 候选身份与研究态术语（Stage-1C 才选；v2 评审已重排为系统的支撑层）

- **UMBRELLA**（TFRL ∩ 冻结 omni ∩ advantage→下一步动作，立项即有）——owner 澄清后 = 主纲领
  对象本身（原第五候选升格）；IAD 2504.01931 = 预登记坍缩风险。
- **I1**（一般 label-free selector）= DIRECT_OCCUPIED（kill 方向保持）→ 系统内终态选择基线。
- **bare-I2** = 机制级被占（scaling-auditory 同核 beam log-lik）、任务格覆盖 MIXED/UNDERSEARCHED
  → audio-grounded evaluator 组件。
- **strict-I2**（I2∩I4 合取）= POST_HOC_NARROWED_CANDIDATE（07-14 后验合成，禁「幸存」框架）→
  评估器组件 + 模态因果探针。
- **I3** → safety/Goodhart/弃权/停止子系统。**I4** = METHOD_FAMILY_OCCUPIED、音频/omni 供给分层
  实例化 UNDERSEARCHED → 测量层（RQ-MEASURE）。
- 否定性结论一律按身份索引 + 强制伴随 token（RESP-02 §3.3）；全局 token 停用；**do_not_claim
  以 RESP-02 §13 机器块为准**。
- **合同**：六份身份合同 + same-selector contract FROZEN@dce5c79（续41）+ 修正案 №1 @0a5e108
  ——作为**组件层合同**继续有效；v2 要求的五份**系统级**合同（black-box/TF/RL/agentic/omni）
  随 system-first proposal 起草。

## 5. selector 子模块线状态（降级为组件 dossier，工件效力不变）

census v2（95 works）+ ledger v2（62 行；discrepancy 五级 MATERIAL 15 / CRITICAL 2 在案）
@28ad858，Gate A 收口 @b1af8c6；Gate B 收口（round-2 协议 v2 六门绿、零查询）@aaffe4c/fcd1c57；
P0-R 计分 = 续42（R6 REOPENED 等）；Survey v2 = ROUND1_SCOUT_COMPLETE（round-1 检索宇宙永久缺失
在案）。承重更正（KIT ST / JudgeBoN=rho_pool / ernez）维持有效。明细一律回 RESP-02
（`2026-07-14-p0r-response-to-remediation-rereview.md`，现行有效）与台账——**不在此复制数字**。

## 6. 正典指针

热层=本文件 · 北极星=[[Project-Thesis]] · 方法论=[[Research-Methodology]] · 记录规约=
[[AI-Collaboration]] · 审计=[[Decision-Log]]（grep 单条）· 取代墓碑=
`archive/research-objective-supersession-index.md` · 死代号=`archive/terminology-tombstones.md` ·
发布快照=`docs/integrity/release_manifest.json` · reviewer 提案与两轮评审=
`2026-07-15-stage1a-research-proposal-*` 系列 · Per-Work=[[Per-Work-Status]]。
