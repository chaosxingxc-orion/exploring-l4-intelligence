# Decision Log

> Append-only, lightweight ADRs — the team's durable **memory**. Entries live in **yearly
> volumes**; append a new decision to the current year's volume (date · what we decided · why ·
> consequences), never rewrite old entries. Load volumes by targeted `rg` only — never broadly.
> Full discipline: [[AI-Collaboration]].

## Volumes

- [[Decision-Log-2026]] — 2026-06-22 → present（含：三阶段方法论、Stage-1 各战役裁决、
  W1–W4 退役、audio-aware evidence acquisition 开题与 GO、公共职能宪章、调研包封存）

## 追加规则 · Append rule

新决策写入当年卷（当前=[[Decision-Log-2026]]）；跨年时新建 `Decision-Log-<year>.md` 卷并在上表
加一行。本页只维护卷索引，不承载条目。
