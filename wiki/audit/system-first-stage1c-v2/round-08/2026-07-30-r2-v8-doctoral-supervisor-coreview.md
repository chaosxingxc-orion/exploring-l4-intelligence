---
title: "R2 开题报告 v8：博导视角隔离复审（round-07 三 MAJOR + 十二 MINOR 的关闭质量专项判定 + 回归扫描）"
date: "2026-07-30"
artifact_type: "REVIEW"
campaign: "system-first-stage1c-v2"
round: "round-08"
review_target: "wiki/survey/workbench/stage1c-portfolio/proposals/2026-07-29-r2-coreview-draft.md"
review_target_commit: "750debf"
review_target_git_blob: "f6507932501a851fad9615dc227f1d0ab8fb5efe（工作树 `git hash-object` 与 `git rev-parse 750debf:<path>` 逐位一致，已核）"
review_target_worktree_sha256: "ea542fd57e1cba371096a411b3ff46d9ac9cabab7796afc623245847b9275195（LF-only，CR=0）"
verdict: "MAJOR_REVISION_REQUIRED"
authority_effect: "WITHHOLD_ONLY_NO_OWNER_DECISION_NO_EXECUTION_GRANT"
human_signature_claimed: false
model_or_metric_execution_authorized: false
stage2a_authorized: false
novelty_verdict: "NOT_ISSUED"
---

# R2 v8 复审：round-07 十五条全部实质关闭，但同一病灶的第四次复现落在 K1b——独立性判据这一支的成本控制无人接手

## 一、评审结论

**裁决：`MAJOR_REVISION_REQUIRED`。round-07 的 3 MAJOR + 12 MINOR 判定全部 CLOSED（无一 PARTIAL、
无一 NOT_CLOSED）；新发现 1 MAJOR + 7 MINOR。**

三句话总结：

1. **本轮的专项任务——round-07 十五条的关闭质量——结论是干净的。** MAJOR-1 的四处（§5.3 阶梯 /
   §6.3 / §6.5 / §7 K1a）逐处落地且互相一致，阶梯是**绑定的**（K1a 的对照对象就是扫到成本可比
   的最高效果档），因此"调度更好 vs 只是搜得更多"这一口在 **K1a 这一支上真的封住了**；MAJOR-2
   的 K5 两级+两条专属前置使"装载判死"（K5-t，受前置与载体双重闸门）与"锚无分辨力"（前置不过 /
   `PENDING_CARRIER`）成为两个可区分状态，§3.4/§5.4/§7 三处同步；MAJOR-3 的 §1.3 载体化改写与
   §4.4 M3 行 / §4.5 / §5.2 三处"无负类"自陈**完全一致**，分桶与 authorization 义务均已落位，
   Decision-Log 续79 的 round-07 执行注记**如实**。十二条 MINOR 逐条按建议或等效方式关闭，§9 的
   逐 id 枚举经本轮脚本重数**闭合**（正文 42 个 id 全部落在声明层内，16/21/6 三个计数正确）。
2. **但 round-07 诊断出的病灶（"范围扩张时量词跟着扩了、检定没跟着扩"）在 K1b 这一支上第四次
   复现，且 v8 的修复恰好没有覆盖它。** v6 的 §6.1 A4a 行原文是"A4a 同 store 同 answerer
   **等成本**、通用自适应 SEARCH 调度"，且 v6 §5.2 明写"`r_consistency`/K2 所需逐步候选答案探针
   （信号分层后为 **A4b 独有** core-call；**A4a 仅在 K2 判定所需限度内使用并同等计费**）……全部
   显式计入对应臂的**等预算**"——round-06 曾逐字认定"`disp` 所需的多次采样已由 §5.2 等预算条款
   覆盖"并判 CLOSED。v8 把 A4a 行的"等成本"改成"成本记账"，并删掉了"A4a 同等计费"那半句，而新建的
   三件成本归因装置（§5.3 阶梯、random-matched-cost、`COST_DISPARATE`）**全部只作用在 K1a 一侧**。
   于是 `A4b − A4a`——§0 与 §8 指定的"音频特有"独立性主张的**唯一**承载——变成一个无成本控制的
   比较：A4b 独享逐步候选答案采样（形态上即自一致性采样，是有文献支撑的准确率提升器）与
   RE_RESOLVE 音频轮次，A4a 二者皆无。K1b 的合取条件②（β 权重非零）管不到这个混淆，因为 β 只
   给 `V̂(RE_RESOLVE)` 定权，β≠0 与"增益其实来自多采样"完全相容（新 MAJOR-1）。
3. **工作记录标记纪律继续合格。** §0–§8 全文 `grep`（`续7N`/`round-0N`/`MAJOR`/`MINOR`/`(owner…`）
   零命中；`owner` 一词的五处命中（第 374/563/565/646/708 行）全部是 §3.4/§5.4/§6.4/§7 的授权
   出口条款（实质内容），`裁定` 二处（第 248/277 行）为研究行文，与 round-07 判法一致。送审件已提交，本评审可绑定不可变 blob `f6507932…`，round-07 的程序性建议已落实。

新 MAJOR 的修复量为半页：要么把探针恢复为**两臂共用的仪器开销**（A4a 同等采样、只是估计量不读
离散度信号——这正是 round-05 信号分层的成本中性写法，且不重新引入任何 cap，不违续79⑤"成本不
设限"），要么把 `COST_DISPARATE` 触发按 K1a 的写法镜像到 A4a/A4b 对上。二者任一即可。

本文是 AI 生成的博导视角隔离复审，不冒充自然人签字，不授予模型/API 调用、数据获取、指标运行、
原型、Stage-2A、创新性结论、push 或 wiki 发布权限；不发 novelty verdict；不翻案 owner 已裁事项，
特别是续79 七点与"研究对象定义权归 owner"边界。

## 二、审查对象与本轮实际核验动作

**Provenance 已修复**：v8 随回应信同 commit `750debf` 提交。工作树 `git hash-object` =
`git rev-parse 750debf:<path>` = `f6507932501a851fad9615dc227f1d0ab8fb5efe`，`git diff 750debf`
对该路径为空；sha256 `ea542fd5…9275195`，LF-only（CR=0）。round-07 只能绑定工作树 sha256 的
程序性缺陷**闭合**。附带事实：v7 的 `git hash-object` 值 `ccfbe7a1…` 对象从未入库
（`git cat-file` 报 could not get object info），故本轮对 v7 的任何陈述只能引 round-08 之前的
评审记录，不能直接对读 v7 blob——这一点在下文凡涉及"v7 曾如何"处均如实标注。

本轮读 round-03..07 五轮评审与各轮回应/关闭记录、Decision-Log 续76/77/78/**续79 全文（含
round-07 执行注记）**、v6 blob（`4b94dd5:<path>`）与 v2 blob（`062c253`），并执行下列**本地证据
抽验**（零网络、零模型/API 调用、零指标运行、零数据集下载、零原型）：

| # | 动作 | 结果 |
|---|---|---|
| 1 | 工作树↔`750debf` blob 一致性（`hash-object` / `rev-parse` / `diff`） | 逐位一致，diff 为空 ✓ |
| 2 | 正文 arXiv id 分层脚本重数（以 §9 为界切分正文/治理节，剥离 id 后再取数字） | 正文 42 个唯一 id，**全部**落在三个声明层内（无游离 id）；深读 16 / 登记未深读 21 / pre-2024 例外 6，**三计数均正确**、三层**无交叠**；反向查得两个"声明而正文未引"的 id（见下） |
| 3 | 反向闭合检查（声明层 → 正文） | `2605.13277` 已被 §9 显式标为"在册但本版未引用，不入枚举" ✓；**`2303.11366`（Reflexion）列在深读 16 篇内，但正文 §0–§8 从未引用**（新 MINOR-3） |
| 4 | v6↔v8 全量数字符号 diff（十进制 + 千分位 + 整数，剥离 arXiv id） | 无任何数字被改写为不同值 ✓；`28.88`/`0.5019` 已恢复（各 2 处）✓；净删项经逐项回查全部落在 VoiceAgentRAG（放弃载体）明细压缩上：`τ=0.40`、`76 chunk`、`316×`（新 MINOR-4）；`3.9 万段/427 小时` 只是 v6 `~39k/427h` 的改写，非新增数字（v6 因 `427h` 无词边界被 round-07 正则漏计） |
| 5 | v6 §6.1 A4a 行与 §5.2 探针条款逐字对读 | v6：**"A4a 同 store 同 answerer 等成本"**；v6 §5.2：**"A4a 仅在 K2 判定所需限度内使用并同等计费……全部显式计入对应臂的等预算"**。v8 §6.1 A4a 行改为"（同库同 answerer，**成本记账**）"，v8 §6.5 只余"A4b 的逐步候选答案探针为**其独有** core-call 成本"，"A4a 同等计费"半句**消失** → 新 MAJOR-1 |
| 6 | round-06 评审 §3.1 对读 | 原文"**`r_consistency` 探针（`disp` 所需的多次采样）已由 §5.2 等预算条款覆盖并要求双读数**"——被 v8 拆掉的正是一条 round-06 判 CLOSED 的识别防线 |
| 7 | 恢复数字的落位核 | `28.88` @ §2.2 SpeechDPR 集成检索率 ✓；`0.5019` @ §2.2 SpeechRAG 低 WER 级联 ✓；`2603.02206` @ §5.1 VoiceAgentRAG 条目（裸名引用消解）✓ |
| 8 | `pdftotext -layout` 抽 `survey-fulltext/2602.10656`（AudioRAG）§2.3 + 附录 A | **两道过滤 = Question Validity Filter + Answer Correctness Filter**；附录 A 的过滤 prompt 原文："If identifying the specific genre is essential to answer, output 'yes'. **If the question can be answered directly from other information provided (without the genre), output 'no'**" → 该过滤删除的是"**不需要音频属性**即可答"的样本，**不是**"不需要外部检索即可答"的样本；第二道只核答案正确性（给 gold audio attributes + 搜索工具）。→ §1.3 的"两个 T1 主载体的构造过滤主动删除负类"在 AudioRAG 上**不成立**（新 MINOR-5） |
| 9 | §0–§8 工作记录标记全文 grep（`续7N`/`round-0N`/`MAJOR`/`MINOR`/`(owner…`/`v[2-8]`） | 零命中（全部命中落在 frontmatter 第 3–9 行与 §9 第 755 行以后）；`owner` **五处**（374/563/565/646/708）均为授权出口条款，`裁定` **二处**（248/277）为研究行文（§2.3 标题、§3 首句）→ **判执行合格，不计条目** |
| 10 | 四处一致性交叉核（§5.3 / §6.3 / §6.5 / §7）与三处一致性交叉核（§3.4 / §5.4 / §7；§1.3 / §4.4 / §4.5 / §5.2） | 见第三、四节逐项 |
| 11 | Decision-Log 续79 执行注记如实性 | 注记原文与 v8 §1.3 的实际处置**逐项对应**（"负例留在实验里"经核实不可实现 / 两 T1 载体构造过滤已删该类样本 / v8 改写为"需新载体或受控注入、本阶段不作主张不设臂" / 裁定意图保留、范围收窄 / owner 生效裁定时可复核）✓；但**⑤ 的名录变更无对应注记**（新 MINOR-1） |
| 12 | `2605.08762`/`2602.10656` 承重数字抽验（`43.44`/`11.72`/`6.56`/`37.0`/`46.2`/`45.0`/三档 `29.06/43.44/44.06`/逐类别四对） | 与 round-07 已源核结论一致，未见回退 ✓；§4.3 的 Gemini-3-Pro 口径标注与两轴分层注记已补 ✓ |
| 13 | §4.3"37 行音频/omni 语料场级事实"的血缘追踪 | 该句为 **v2（`062c253` 第 112 行）原文**，v3–v6 无，v7/v8 恢复；其"场级事实"底座可追到 `…/stage1c-portfolio/2026-07-26-d4-agentic-system-dossier-draft.md` 与 d2 dossier → 血缘=round-03，但 §9 的"五处变化"只申报了 §5.1 一处 v2 恢复（新 MINOR-4 并入） |

## 三、round-07 三项 MAJOR 的关闭质量逐项判定

### 3.1 MAJOR-1（K1a 成本归因）—— **CLOSED**

四处逐项核，**互相一致**：

| 位置 | v8 文本要点 | 判定 |
|---|---|---|
| §5.3 须复现 | 固定档"论文三档起步，**向上扩档直至该档实测成本（至少 retrieval hops 与 core calls 两维的均值与 P95）不低于自适应臂的实测成本**，保证'效果最强固定策略'不是被论文档位人为封顶的天花板" | 与 round-07 建议 (a) 逐项对应 ✓（锚点措辞见新 MINOR-6） |
| §5.3 对照臂 | "**random-matched-cost（与自适应臂等实测成本的随机调度）保留为最低归因对照**——它回答'同样的量、随机地花'能否达到同样效果……这是效果归因对照，不是预算优化；等预算读数/Pareto 分析仍降为后期诊断" | 恢复到位、并自带与续79⑤ 的切割论证 ✓（登记问题见新 MINOR-1、判据角色见新 MINOR-2） |
| §6.3 模块表 | retrieval planner 行"最低对照 = best fixed / random matched-cost / always与never 角点 / A4a" | 与 §5.3 **镜像一致**，round-07 MINOR-4 的两表冲突消解 ✓ |
| §6.5 效率层 | "双读数……保证臂间成本口径可比（**无预算 cap 制下双读数的作用是口径透明**，效果侧的成本归因由 §5.3 固定档阶梯、random-matched-cost 对照与 §7 的 `COST_DISPARATE` 触发承担）" | round-07 指出的"无所指的后半句"已删并显式移交职能 ✓ |
| §7 K1a | "固定档阶梯按 §5.3 向上扫至成本不低于 A4a，取其中效果最高档×选定 re-resolve 固定档；并列报告论文 agentic SOTA 臂与 random-matched-cost 归因对照……**成本悬殊诚实触发**：若自适应臂实测成本超出对照阶梯最高档的预注册倍数，该读数标注 `COST_DISPARATE` 单独报告，不得单独支撑调度杠杆结论" | 建议 (b) 到位；且倍数已入 §9 authorization 清单 ✓ |

**归因口是否封住（K1a 一支）**：是。关键在于阶梯**进入了判据本身**——K1a 的比较对象被定义为
"扫到成本不低于 A4a 的阶梯内效果最高档"，因此若 A4a 仍胜出，"只是搜得更多"这一解释在构造上被
排除；这比 v6 的"等预算"更强（v6 是给处理臂封顶，v8 是把基线做强），且完全落在效果优先纲领内。

**与续79⑤ 是否冲突**：**实质不冲突，名录层有一处未登记的偏离。** ⑤ 的实质是三条：主判据=相对
最强固定策略与已发表 agentic 基线的效果绝对提升 / 成本九维只报告不设限 / 定价与最优化归后期。
v8 三条全部保持：处理臂无 cap（硬上限降为工程护栏）、matched-cost 是让**随机基线**去匹配自适应臂
的**实测**成本（不约束处理臂）、policy 无 λ·c、§6.5 仍把等预算读数/Pareto/等成本最优质量列为后期
诊断。但续79⑤ 的降级名录**逐字点名了 random-matched-cost**，v8 把它升回主判据侧的"最低归因对照"，
这一处名录变更只在回应信里声明"不冲突"、在 §5.3 里自证，**未按 ④ 的先例在 Decision-Log 续79 附
执行注记**（新 MINOR-1）。裁定权归 owner，本文不代裁。

### 3.2 MAJOR-2（K5 分辨力与两级）—— **CLOSED**

三处一致性核：

- **§7 K5**：`K5（装载对照，阶段二；仅 T2；两级判定）` 明写主载体检定①②在锚上不可构造，故
  **自带两条专属前置**（㈠非语义槽判别 query 占比 ≥ 预注册下限；㈡O1 的 R@k 距天花板有预注册
  余量），`K5-r` 检索层=按面分层报告，`K5-t` 任务层携判死条款且**仅在具备非语义槽需求的载体上
  生效，到位前记 `PENDING_CARRIER`、不判死**；"前置任一不过 → K5 不触发任何判死，回 owner 重议
  载体（与 §5.4 梯④同型出口）"。
- **§3.4 第 3 项 + 诚实声明**：判据句已两级化（K5-r/K5-t 同义复述、PENDING_CARRIER 同义复述），
  并保留"情感面 key 只做检索层读数、不做任务层主张"的原纪律，与 K5 两级**同构**；末句"锚载体本身
  无分辨力（K5 专属前置不过）时不判死、回 owner（§5.4/§7）"。
- **§5.4 梯**：④ 之后新增"**O 臂的锚载体（Spoken-SQuAD/SLUE-SQA-5）失敏时同用梯④出口**：K5 不
  触发判死、回 owner（其专属分辨力前置见 §7 K5）"。

**"装载判死"与"锚无分辨力"是否可区分**：是，且是**双闸门**可区分的——(i) 前置㈠㈡ 不过 →
状态="载体无分辨力"，出口=回 owner，不产生任何判死；(ii) 前置过但载体不具非语义槽需求 →
`K5-t = PENDING_CARRIER`，同样不产生判死；(iii) 前置过 + 载体合格 + O2 无增益 → 才是"装载升级不立
研究主张"。round-05 MAJOR-B 所防的"无分辨力读数被记作判死"形态在新臂上被堵住。

**K5-t 生效条件是否可判定**：**基本可判定，但绑定不显式**。前置㈠ 是一个带预注册下限的可测量
（锚上非语义判别槽 query 占比），而 K5-t 的生效条件写成"具备非语义槽需求的载体"——两者读来是同一
构念，但件内未声明其为同一阈值，存在被实施为**两个独立构念**的空间（新 MINOR-7）。另有一处措辞：
§3.4/§7 称 K5-r 为"当前锚载体唯一支持的**判定**层"，而 K5-r 实际不携任何判死条款（是报告层），
"判定"一词与 §7 内"判死"用法易混。

### 3.3 MAJOR-3（信息角色边界载体化）—— **CLOSED**

四处一致性核，**无残留矛盾**：

| 位置 | v8 文本 | 与 §1.3 一致？ |
|---|---|---|
| §1.3 | "两个 T1 主载体的构造过滤**主动删除**了不需要外部信息即可回答的样本……其上全部实例按构造落在主张分布内，**边界自动满足**；相应地，'该学会不搜'的负类实例在现有载体上**按构造不存在**——不是被我们排除，是载体没有……需具备负类的新载体或受控注入协议，**本阶段不作主张、不设臂**" | — |
| §4.4 M3 行 | "载体无负类，不可直测" | ✓ 一致（"按构造不存在"⊃"不可直测"） |
| §4.5 明确不研究 | "部署期 need-detection 的通用检测器（载体无负类，不可测）" | ✓ 一致 |
| §5.2 readiness | "无检索快照/负类/dev-test" | ✓ 一致 |

round-07 指出的三项后果逐项消解：(1) "不被排除出实验"这句空陈述**已删**，改为事实陈述；
(2) "over-search 一半"的说法**已删**，并新增显式切割"此边界与 K2 的 over-search 判据无关：K2
度量的是回合内'已拿到正确答案后仍继续搜'，不需要负类实例"——与 §7 K2 的 `t*` 回合内定义相容 ✓；
(3) 安全阀不再只是声明：分区职能移交给**载体构造事实**（可从数据集构造读出，可判定性与 v6 的
槽型边界同级），实例级标注协议作为**条件项**入 §9 authorization 清单（"仅当引入负类载体或受控
注入时"），§6.5 有效性层补"（信息角色可判定的载体上另按信息角色分桶，§1.3）"。分桶与登记**均已
落位** ✓。§8"对象=外部知识 action family 专用"一行未受影响 ✓。

**续79④ 执行注记如实性**：注记（本轮读原文）与 v8 实处置逐项对应，且明写"裁定意图保留、适用范围
如实收窄，owner 生效裁定时可复核该处置"——**如实，不构成 backdating，不构成代 owner 改裁**。

**唯一残留（源核发现）**：§1.3 的载体事实句对**两个** T1 载体作了同一断言，而本轮 PDF 源核显示
AudioRAG-500 的两道过滤删除的是**不需要音频属性**即可答的样本（附录 A prompt 原文），不是不需要
外部检索即可答的样本；其"外部信息高度重要"来自**构造设计**（多跳外部知识题）而非删负类的过滤器。
该载体现正承载 K3 判据、且 §5.4 梯③ 可把它升为主载体，故此处的事实精度有判据后果 → 新 MINOR-5。

## 四、十二项 MINOR 逐条验收

| # | round-07 要求 | v8 实处置（位置） | 判定 |
|---|---|---|---|
| MINOR-1 | §9 两句自我声明改为如实；第五处变化如实登记并注明血缘 round-03 | §9 末段"**数字保真如实声明**：v6→v8 无任何数字被改写为不同值；v7 曾净删两数（0.5019/28.88）与两个 id（2603.02206/2303.11366），**v8 已全部恢复**；净增两数经 round-07 本地源核为真"；"**相对 round-06 已审范围的变化五处**"(a)–(e)，(e) 明注"§5.1 四问块自 v2 恢复（评审血缘=round-03，其承重数字经 round-07 逐项源核）"。恢复落位经动作 7 核实 | **CLOSED**（声明穷尽性仍差三数一句 → 新 MINOR-4，同型第三次） |
| MINOR-2 | §4.3 补 Gemini-3-Pro 口径 + 两轴不可并称"类别" | §4.3 第 3 点："**该消融全表为 Gemini-3-Pro 口径，本核口径见 §5.4 不得串用；IMAGE/VIDEO 属检索目标轴、SPEECH/AMBIENT 属音频内容轴，两轴样本分层不同**" | **CLOSED**（160/120 具体分层量未写，但"两轴样本分层不同"已足以阻断并称，无结论依赖该数） |
| MINOR-3 | "每格约 4 题"→ ≈40/类 | §5.1："温度 0 单样本、**15 细类每类约 43 题**——统计分辨率撑不起 12 模型排名"；与 640/15 内部自洽（640/15≈42.7 ✓） | **CLOSED** |
| MINOR-4 | §5.3↔§6.3 二取一并镜像 | §5.3 保留为"最低归因对照"，§6.3 retrieval planner 行同列 `random matched-cost` → 两表镜像 | **CLOSED**（"若保留须说明其判据角色"这半句未兑现 → 新 MINOR-2） |
| MINOR-5 | §3.4 补路由/融合确定性规则并入 §9 同一 authorization 项 | §3.4："query 面路由=**确定性两选一、authorization 预注册**：冻结核在固定 prompt schema 下自报槽型标签选面，或不路由、查全部面后按预注册权重取最大相似度融合"；§9 同项括注"及 query 面路由/融合规则（§3.4 两选一的选定与参数）"；audio2vec 改为"现成音频事件/场景向量模型如 audio2vec 一类——具体编码器选型=authorization 义务" | **CLOSED** |
| MINOR-6 | "各面被压进同一向量"改读集内事实句 + 明写未测量 | §2.1："**key 全部是单向量**——每个 value 只有一个 key、只服务一个匹配面。至于其余面……是被压缩在该向量里还是被丢弃（SpeechDPR 的 key 蒸馏自文本稠密检索 teacher，'丢弃'同样可信），**读集内没有测量**"；§3.3 同步；§3.4"正是 O 臂检索层读数要回答的" | **CLOSED**（且按建议把 O2 立论从推断改成待测问题） |
| MINOR-7 | §4.4 末段给主杠杆机制位 | §4.4 末段："**主杠杆（§4.5 双源动作调度）借的是 M2/M4 谱系的效果形态**——'选择性获取'的前瞻价值估计本身（M6 的负信息增益与 VOI-search'中预算有时胜高预算'的花费-效果非单调证据……）；只有其**定价/成本最优化形态**（M2 的 value-per-budget 归一、M8 的预算再分配）降为后期参考。次杠杆取 M1/M5/M6/M7 的效果形态" | **CLOSED**（§4 叙事与 §4.5 主次排序自洽） |
| MINOR-8 | 重数并补齐四个游离 id 的层级；处置 2605.13277 | §9 改逐 id 枚举，四个 id 均入登记未深读层；`2605.13277` 标"在册但本版未引用，不入枚举"。**本轮脚本重数**：正文 42 id 全部在册于声明层、三计数 16/21/6 正确、层间无交叠 | **CLOSED**（反向发现 `2303.11366` 枚举而未引 → 新 MINOR-3） |
| MINOR-9 | §3.2 补 key/value 段 | §3.2 新增整段："Wiki-LLaVA/EchoSight 的 key=实体名、value=百科文档；element/scene 两级证据单元（2605.15019）即同一场景 value 的**两层 key 粒度**；层次视觉记忆（2605.16481）=在线构建的多层 key。读集内视觉域出现了'同一 value 多层（粒度）key'，但**未见跨模态多面 key**——语音域 O 臂要补的正是后者" | **CLOSED**（并回答了 §0 表为阶段二设的"图像域怎么组织"之问，且把 O 臂空位论证接上跨域对照） |
| MINOR-10 | §9 补类别型槽分类体系与别名表 | §9 清单："**各类别型槽的分类体系与别名表（来源/版本/冻结方式——§6.2 类别槽 corr/confl 确定性的前提）**" | **CLOSED** |
| MINOR-11 | K2 补回抽样率预注册 + 非全量判定诚实句 | §7 K2："抽样率与判官协议=**authorization 预注册**。如实标注：这是'答案轨迹级判定+抽样复核'的事件率估计，**非证据内容级全量判定**（升级路径见 §6.4 可选请求）"；§9 judge 合同项括注"（含 K2 抽样率与判官协议）" | **CLOSED** |
| MINOR-12 | §1.2 补槽型括注；§6.2 恢复 `A_{1..t-1}` | §1.2："（entity-only oracle 33.76 / 端到端 43.44 / gold-entity 50.00，仅 Gemini-3-Pro 口径；**此为实体槽证据，其他槽型的 headroom 分解读集内无载体**）"；§6.2 `state_t` = "…**动作历史 A_{1..t-1}** 与各动作后的候选答案漂移记录" → `V̂_gen` 的 `nov(q|A_{1..t-1})` 符号已有绑定处 | **CLOSED** |

## 五、新发现

### 新 MAJOR-1（§6.1 A4a 行 + §6.2 state_t + §6.5 + §7 K1b；来源=变化 (c) 的未申报第二面）
### K1b 的 `A4b − A4a` 失去成本控制，且探针是"A4b 独有开销"还是"两臂共用仪器"件内自相矛盾——独立性主张的识别因此不可判定

**问题。** MAJOR-1 的病灶（"拆掉约束面，判定职能无人接手"）在 K1b 这一支上复现，而 v8 的修复按
round-07 自己的建议范围只覆盖了 K1a。

1. **被拆掉的约束面（有 blob 证据）。** v6 §6.1 的 A4a 行首句是"A4a 同 store 同 answerer
   **等成本**、通用自适应 SEARCH 调度"；v6 §5.2 的探针条款是"`r_consistency`/K2 所需逐步候选
   答案探针（信号分层后为 **A4b 独有** core-call；**A4a 仅在 K2 判定所需限度内使用并同等计费**）
   与 admission 侧 `confl/srcQual` 的计算调用，**全部显式计入对应臂的等预算**（维度与 cap 形式
   预注册），并报告双读数"。round-06 评审 §3.1 逐字认定："**`r_consistency` 探针（`disp` 所需的
   多次采样）已由 §5.2 等预算条款覆盖并要求双读数**"，并据此判 MINOR-3/MINOR-e CLOSED。
   v8 §6.1 A4a 行改为"（同库同 answerer，**成本记账**；……）"，§6.5 只余"A4b 的逐步候选答案探针
   为**其独有** core-call 成本"，**"A4a 同等计费"整句消失**。
2. **新建的三件装置都不覆盖它。** §5.3 阶梯的对象是"固定档 vs 自适应臂"；random-matched-cost 的
   对象是"随机调度 vs 自适应臂"；§7 的 `COST_DISPARATE` 触发条件是"自适应臂实测成本超出**对照
   阶梯最高档**的预注册倍数"。三者一律以"固定/随机基线 ↔ 自适应臂"为轴，**没有一条以 A4a ↔ A4b
   为轴**。§7 K1b 的两个合取条件（差分 95% 下置信界 >0 ∧ β 权重与零可区分）也均无成本项。
3. **后果：差分含义重新变得多义。** A4b 相对 A4a 独享两类开销——(i) 逐步候选答案探针（`maxAgree`
   的可实现定义是"**已采样候选答案**中语义槽众数占比"，即每步对冻结核多次采样，形态上就是
   自一致性采样，是有文献支撑的一阶准确率提升器）；(ii) `RE_RESOLVE` 的音频轮次（A4a 只有
   dev 选定的固定档 {0, 至多 1 次前置重听}）。因此 `A4b − A4a > 0` 存在一个**未被任何装置排除**
   的替代解释："A4b 只是多打了几倍 core call"。K1b② 挡不住：β 只给 `V̂(RE_RESOLVE)` 定权，
   β≠0 意味着重听动作被赋值，与"净增益其实来自多采样"完全相容。而 §0 与 §8 都把独立性主张
   **唯一**挂在这个差分上——这正是 owner 即将落笔的"R2 独立 / MERGE 入 R6-R8"所依赖的读数。
4. **且件内对探针归属自相矛盾，两种读法后果不同。** §6.2 的 `state_t` 是**两臂共用定义**，其中
   已包含"含各假设自一致性计数 `agree(h)`"与"各动作后的候选答案漂移记录"——按此读法探针是共用
   仪器，混淆被消解，但 §6.5 的"**其独有** core-call 成本"就是错的；按 §6.1"A4a 无任何感知
   不确定性输入"+§6.5"A4b 独有"读法，混淆是活的。件内**未在两读法间落定**，故最承重的那个差分
   的识别**作为文本不可判定**——无论采哪一读，都必须动笔。
5. **申报层同步缺一项。** §9 的变化 (c) 只写"K1a/评价层从等预算对照改为'固定档阶梯扫至成本可比
   +random-matched-cost 归因对照+COST_DISPARATE 触发'（K1b/K2/K3/K4 语义不变）"。但 A4a 行由
   "等成本"改为"成本记账"、探针由"入等预算 cap"改为"入账"，是**影响 K1b 识别条件**的第二面范围
   变化，未被 (a)–(e) 任何一项覆盖；"K1b 语义不变"在判据文本层为真，在**识别条件**层不为真。

**建议修复（三条互斥，任一即可；均不重新引入 cap、不违续79⑤）。**
(a) **仪器共用派（最省、且与 round-05 信号分层同构）**：声明逐步候选答案探针为**两臂共用的仪器
开销**——A4a 同等采样、同等计费，只是其估计量 `V̂_gen` 不读 `maxAgree/disp/audCons`（信号分层的
本意就是"信号使用面分层"而非"信号采集面分层"）；同时把 §6.5 的"其独有"改为"A4b 独有**使用**、
两臂同等**采集与计费**"。
(b) **触发镜像派**：把 `COST_DISPARATE` 按 K1a 的写法镜像到 A4a/A4b 对——预注册 core-call 与
audio-seconds 的比值上限，超出即标注该差分为成本悬殊、不得单独支撑独立性主张（与 K1b② 并列为
第三个合取/诚实条件）。
(c) **对照臂派**：新增 `A4a+`（A4a 加同量探针调用、信号采集后丢弃不进估计量）作为 K1b 的
matched-cost 对照，差分改挂 `A4b − A4a+`。
无论采哪条，§9 变化 (c) 须补记"A4a/A4b 等成本约束的处置"。

---

### 新 MINOR（七项）

| # | 位置 | 问题 | 最小修复 |
|---|---|---|---|
| 新 MINOR-1 | §5.3 / §9 变化 (c) / Decision-Log 续79 | 续79⑤ 的降级名录**逐字点名** random-matched-cost，v8 将其升回主判据侧"最低归因对照"。实质不违⑤（处理臂无 cap、主判据仍是绝对效果提升、Pareto/等预算读数仍在后期），但这是对 owner 已签名录的一处偏离，只在回应信与 §5.3 自证，**未按 ④ 的先例附 Decision-Log 执行注记**——同一份裁定的两处偏离，一处登记一处不登记 | 按 ④ 的写法在续79 附一条执行注记（"⑤ 中 random-matched-cost 经 round-07 MAJOR-1 认定承担效果归因职能，v8 恢复为最低归因对照；等预算读数/Pareto 仍降后期；owner 生效裁定时可复核"），或在 §9 变化 (c) 标注"该项相对续79⑤ 名录为执行层偏离，待 owner 复核" |
| 新 MINOR-2 | §5.3 / §7 K1a | matched-cost 被定性为"**最低归因对照**……是把'调度更好'与'只是搜得更多'分开的装置"，但 §7 K1a 只把它列为"并列报告"，判死条件仅写在"效果最强固定档"上——若 A4a 胜过成本可比的最强固定档而**不**胜过 random-matched-cost，件内无任何规则。round-07 MINOR-4 的备选路径明写"若保留在最低对照列，须说明它此时是记账诊断而非判据输入"，v8 选了"是归因装置"却未给判据角色（对照：K1b② 在 round-06 被专门升为合取条件才成为绑定护栏） | 二选一：①升为 K1a 的合取/诚实条件（不胜 matched-cost 即标注不得单独支撑调度杠杆结论）；②降回"报告项/离线诊断"并相应弱化 §5.3 的"归因装置"定性 |
| 新 MINOR-3 | §9 深读层枚举 | `2303.11366`（Reflexion）列入"深读层 16 篇"，但正文 §0–§8 **从未引用**（本轮脚本双向核）。这与同段自设纪律"分层按'**正文实际引用**'逐 id 枚举，可自校验"直接冲突，且与 `2605.13277` 的处置**不对称**（后者因未引用被明确排除出枚举）。成因可追：MINOR-1 要求"恢复被删的两个 id"，v8 把它恢复进了 §9 枚举而非正文 | 二选一：①按 `2605.13277` 同法处置（移出枚举、注明在册未引用，深读层记 15 篇）；②在正文恢复 Reflexion 的机制定位引用（v6 曾有其位）。同时把"16 篇"计数与所选方案对齐 |
| 新 MINOR-4 | §9 数字保真声明 + 变化五处 | 声明仍不穷尽（同型第三次）：(i) v6→v8 另净删三数 `τ=0.40`、`76 chunk`、`316×`（全部出自 VoiceAgentRAG 明细，该载体已"放弃"、仅作 R9 参考，故**无语义弱化**，压缩本身属 owner 可读性裁定授权范围）；(ii) §4.3 的"37 行音频/omni 语料场级事实"是**第二处 v2 恢复**的承重括注（v2 `062c253` 第 112 行原文；v3–v6 无；底座可追 d4/d2 dossier），而变化 (e) 只申报了 §5.1 一处 | 把声明改为可证成的形态："承重数字零漂移；非承重的 VoiceAgentRAG 明细按可读性裁定压缩（列出三数）"；变化 (e) 改为"§5.1 四问块**与 §4.3 场级事实括注**自 v2 恢复（血缘=round-03）" |
| 新 MINOR-5 | §1.3 载体事实句 | "**两个** T1 主载体的构造过滤主动删除了不需要外部信息即可回答的样本"——本轮 PDF 源核：AudioRAG-500 的两道过滤是 Question Validity + Answer Correctness，附录 A 的 prompt 原文删除的是"**can be answered directly from other information provided (without the genre)**"即**不需要音频属性**即可答的样本（补集，方向相反）；第二道只核答案正确性。AudioRAG 的外部信息必要性来自**构造设计**（多跳外部知识题），不是删负类的过滤器。该载体现承载 K3、且 §5.4 梯③ 可将其升为主载体，故"边界自动满足"的保证强度在两载体上不同 | 分述两载体："Omni-DeepSearch-640 由构造过滤保证（点名'removes samples answerable … without retrieval'那道 GPT-5 过滤）；AudioRAG-500 由构造设计（多跳外部知识题）保证、**无删负类过滤器**，故其上的边界满足为设计级而非过滤级"；并在 §5.4 梯③ 注明载体升格时该保证强度同步改写 |
| 新 MINOR-6 | §5.3 vs §7 K1a | 阶梯锚点措辞不一：§5.3 写"不低于**自适应臂**的实测成本"（未指明哪个），§7 写"扫至成本不低于 **A4a**"。A4b 通常更贵（探针+重听），两读法给出不同强度的基线。另 `COST_DISPARATE` 的触发域未声明：阶梯按定义扫到"成本 ≥ 自适应臂"为止，故该触发只能在**阶梯无法扫到成本可比**（档位/API/工程上限耗尽）时才有域，否则构造性空转 | §5.3 改为"不低于 **A4a**（K1b 侧的 A4b 成本另按逐臂报告）"；`COST_DISPARATE` 补一句触发域："当阶梯因档位/工程上限无法扩至成本可比时适用" |
| 新 MINOR-7 | §7 K5 / §3.4 | (a) K5-t 的生效条件"具备非语义槽需求的载体"与前置㈠"锚上非语义判别槽 query 占比 ≥ 预注册下限"读来是同一构念，但件内未声明其为同一阈值，留有被实施成两个独立构念的空间；(b) §3.4/§7 称 K5-r 为"唯一支持的**判定**层"，而 K5-r 不携任何判死条款（是报告层），与 §7 内"判死"用法易混 | (a) 明写"K5-t 的载体合格性=前置㈠ 同一阈值判定"；(b) K5-r 改称"报告层/读数层"，或补一句"K5-r 不产生判死，仅提供按面分层证据" |

## 六、任务书四项审查的逐项结论（便于 owner 对照）

| 审查项 | 结论 |
|---|---|
| 1 三 MAJOR 关闭质量 | **三项全 CLOSED**。MAJOR-1：四处一致且阶梯**绑定进判据**，K1a 一支的归因口封住；与续79⑤ 实质不冲突（名录层一处未登记偏离→新 MINOR-1）。MAJOR-2：两级+双闸门使"装载判死"与"锚无分辨力"可区分；K5-t 生效条件基本可判定但绑定不显式（新 MINOR-7）；§3.4/§5.4/§7 三处一致。MAJOR-3：§1.3 与 §4.4/§4.5/§5.2 **四处一致**、分桶与 authorization 均落位、与 K2 切割成立、续79 执行注记如实；残留=AudioRAG 载体事实精度（新 MINOR-5） |
| 2 十二 MINOR 验收 | **十二项全 CLOSED**（无 PARTIAL、无 NOT_CLOSED）。§9 逐 id 枚举经脚本重数**双向闭合于正文侧**（42 id 全部在册、16/21/6 计数正确、层间无交叠）；反向发现一个枚举而未引的 id（新 MINOR-3）。恢复的 `28.88`/`0.5019`/`2603.02206` 落位正确、VoiceAgentRAG 裸名引用消解 |
| 3 新回归扫描 | 本轮改动的十六节全扫。**零数字被改写为不同值**；无新增未源核数字（`3.9 万段/427 小时` 经核为 v6 `~39k/427h` 的改写，非新增）。发现的回归有二：**识别条件层**的 A4a/A4b 等成本约束丢失（新 MAJOR-1，v6 blob + round-06 判定双证）与 §9 声明穷尽性（新 MINOR-4）。§5.3/§6.3/§6.5/§7 与 §3.4/§5.4/§7 与 §1.3/§4.4/§4.5/§5.2 三组交叉一致性**均无新矛盾** |
| 4 工作记录标记纪律 | **§0–§8 仍零标记**，判执行合格、不计条目（`owner` 五处均为授权出口条款，`裁定` 二处为研究行文） |

## 七、专项自审：本轮是否越界

- **是否推翻续79 七点裁定？否。** 新 MAJOR-1 落在⑤的**执行方式**（不要求恢复任何 cap，三条备选
  修复中最省的一条只是把探针改为两臂共用仪器开销，处理臂仍无预算上限）；新 MINOR-1 只要求把
  已发生的名录偏离**登记给 owner 复核**，明确不代裁其成立与否；其余 MINOR 均为文书/绑定/精度层。
  研究对象定义权、O 臂选型、效果优先纲领、假设泛化四项一律按 owner 口径适用。
- **是否重构研究对象？否。** 未提出任何新臂（新 MAJOR-1 的 (c) 备选臂 `A4a+` 是三条互斥修复中
  最贵的一条，本文优先推荐不新增臂的 (a)）、未改载体、未改红线、未改 K 判据的判死方向。
- **是否提前索要 authorization 层义务？否。** 无一条要求 SESOI/power/test n/judge 保真数值/
  数据集 lock/检索服务 pin/dev 数据。K1–K5 数值、`COST_DISPARATE` 倍数、K5 两条前置的下限与
  余量、抽样率、别名表、检查点核查一律确认继续推迟。
- **是否翻案前七轮已认可的推迟项？否。** 唯一"回看"是新 MAJOR-1 引用 round-06 对探针成本条款的
  CLOSED 判定，用途是证明该防线曾经存在并被拆除，不是要求恢复等预算制度。
- **是否发 novelty verdict？否。** `novelty_verdict: NOT_ISSUED`；本文不对 R2 的技术新颖性作任何
  肯定或否定表述。
- **是否越界执行？否。** 零网络、零模型/API 调用、零指标运行、零数据集下载、零复现、零原型；
  仅本地已落盘资产读取（AudioRAG PDF 文本抽取落会话 scratchpad）、Git blob 读取与比对、
  本地脚本重数。未修改送审件或任何其他仓库文件，未 add、未 commit、未 push。

## 八、目的链、Provenance 与失效条件

**结论。** v8 对 round-07 十五条的关闭是**实质性**的：三项 MAJOR 各自的判据洞都被补上了可判定的
检定或可核实的载体事实，十二项 MINOR 逐条按建议或等效方式关闭，§9 的枚举第一次**可自校验**，
数字**零漂移**，工作记录标记纪律连续两版合格。若只按任务书的专项范围判，本轮应当是 `MINOR_REVISION`。
本轮判 `MAJOR_REVISION_REQUIRED` 的**全部**理由是一处新发现：round-07 诊断的病灶——"范围扩张时
量词跟着扩了、检定没跟着扩"——在 `A4b − A4a` 这一支上第四次复现。v6 用"A4a 等成本 + 探针入等预算
（A4a 同等计费）"承担这一支的成本控制，round-06 曾判该防线 CLOSED；变化 (c) 拆掉了它，而 v8 新建的
三件装置全部以"基线 ↔ 自适应臂"为轴，**没有一条以 A4a ↔ A4b 为轴**。这一支恰好是 owner 落笔
"R2 独立 / MERGE" 所唯一依赖的读数，且件内对探针归属（§6.2 共用 `state_t` vs §6.5"A4b 独有"）
自相矛盾，故其识别**作为文本不可判定**。修复量半页，最省的一条修复不新增臂、不新增 cap、不违
续79⑤。

**续77② 生效条件的判定。** 按任务书要求明确表态：本轮**不判该条件成就**。理由不是十五条的关闭
质量（那是合格的），而是存在一项新 MAJOR，且它落在独立性判据本身的识别条件上——而续77② 的
生效裁定正是要对"R2 是否独立"落笔。建议 owner 在新 MAJOR-1 关闭（三条备选任一）后再落生效裁定；
若 owner 认为该项属执行合同层可延后事项（本文认为不宜，因为它改变的是 K1b 读数的**含义**而非
其数值），则应在裁定文本内显式记载"K1b 差分的成本可比性处置留待 Stage-2A 执行合同"，使该风险
不被生效裁定隐性吸收。

**推理摘要。** 本轮先把"关闭是否真实"查干净：三 MAJOR 各按其四处/三处/四处一致性面逐处对读，
十二 MINOR 逐条定位到 v8 的实际句子，再用两个脚本做机械校验（id 分层双向闭合、v6↔v8 全量数字
diff）。这一块的结论比预期更好：不仅零漂移，§9 的枚举第一次真的可自校验，而 MINOR-6/MINOR-9 的
关闭还顺带把 O2 的立论从"推断"改成"待测问题"并给它接上了跨域对照，是实质增强而非文书敷衍。
第二块把 round-07 的病灶诊断当作**假设**去检验："变化 (c) 拆掉的约束面，是否还有第二个未被认领的
判定职能？"——答案是有：v6 的"等成本"承担着两个职能，一个对 K1a（基线不被人为封顶），一个对 K1b
（差分不被多打 core call 解释）。round-07 只追到第一个，v8 也只修了第一个。这条线索是从 v6 blob 的
A4a 行首二字（"等成本"→"成本记账"）和 v6 §5.2 那半句"A4a 仅在 K2 判定所需限度内使用并同等计费"
的消失读出来的，两者都不在 §9 的五处变化申报里；round-06 评审 §3.1 的原句提供了"该防线曾被判
CLOSED"的第三方证据。另有一处形态复现值得单记：§9 的自我声明穷尽性这次仍差三数一句，已是第三次
（round-05 MAJOR-C → round-07 MINOR-1 → 本轮新 MINOR-4）——本轮因被删项全部落在"放弃"载体的
非承重明细上而判 MINOR，但**同一形态第三次出现**本身值得 owner 注意：问题不在数字，在"申报口径
先写后核"的习惯。

**目的链。** 为了让 owner 在"R2 独立 / 合并入 R6-R8"之间落笔时其裁定能被后续实验证实或证否；
所以每一个能改变该裁定的读数都必须有一个**含义唯一**的比较来承接；所以本轮在确认 round-07 十五条
已被实质关闭之后，把剩余审查全部收敛到"新建的归因装置覆盖到哪些比较、漏掉哪些"，而不再重复审查
文献充分性（round-03）、臂结构存在性（round-04）、差分含义定义（round-05）、关闭度（round-06）与
保真回归（round-07）——那五者本轮抽验均未见回退。

**Provenance。** 本文审查 commit `750debf` 的 blob `f6507932501a851fad9615dc227f1d0ab8fb5efe`
（工作树逐位一致、LF-only、sha256 `ea542fd5…9275195`）。判断依据：round-03..07 五轮评审与各轮
回应/关闭记录（`…/round-03/`…`…/round-07/`，其中 round-07 两件为本轮对照基准）；owner 裁决
`wiki/Decision-Log.md` 续76/续77/续78/**续79（含 round-07 执行注记）**；历史 blob `062c253`(v2)
与 `4b94dd5:<path>`(v6)；v7 的 `git hash-object` 值 `ccfbe7a1…` 经 `git cat-file` 确认**对象未入库**，
故本文凡涉 v7 之处一律以 round-07 评审记录为据、不声称直接对读 v7；以及第二节表列的十三项本地
核验（blob 一致性、id 分层双向脚本重数、v6↔v8 全量数字 diff、恢复数字落位核、v6 A4a 行与 §5.2
探针条款逐字对读、round-06 §3.1 对读、AudioRAG PDF §2.3 与附录 A 过滤 prompt 源核、续79 注记
如实性核、承重数字抽验、全文标记 grep、v2 血缘追踪）。本轮 exposure：零网络检索、零模型/API 调用、
零指标运行、零数据集下载、零复现、零原型；新增动作仅为本地已落盘资产读取、PDF 文本抽取（落会话
scratchpad，未写入仓库）、Git blob 读取与比对、本地脚本重数。未对送审件或任何其他仓库文件作出
修改，未 add、未 commit、未 push。本文为审计层记录，不得原位改写。

**失效条件。** 若 v8 被修订为 v9，本文对新 MAJOR-1 与七项新 MINOR 的描述按新 blob 重新核验；在
未触动 §1.3/§3.4/§5.3/§6.1/§6.2/§6.5/§7/§9 的前提下，本文对 round-07 十五条的 CLOSED 判定继续
有效。若 owner 直接裁定改变研究对象定义、载体、红线边界或续79 任一裁定（对象定义权归 owner），
本文中依赖旧口径的条目按新裁决作废，但**已核验的证据事实**独立于处置结论继续有效，计：
工作树 = `750debf` blob `f6507932…`（sha256 `ea542fd5…`，CR=0）；v7 对象 `ccfbe7a1…` 未入库；
v8 正文 42 个 arXiv id 全部落在 §9 三个声明层内、层间无交叠、`16/21/6` 三计数正确；`2303.11366`
与 `2605.13277` 为声明层内正文未引的两个 id（后者已显式排除、前者未）；v6→v8 无任何数字被改写为
不同值，净删 `τ=0.40`/`76`/`316`（均出自 VoiceAgentRAG 条目），`3.9 万段/427 小时` = v6 `~39k/427h`
之改写；v6 §6.1 A4a 行首句含"**等成本**"、v6 §5.2 含"**A4a 仅在 K2 判定所需限度内使用并同等
计费……全部显式计入对应臂的等预算**"，v8 二者均无；round-06 评审原句"`r_consistency` 探针
（`disp` 所需的多次采样）已由 §5.2 等预算条款覆盖并要求双读数"；AudioRAG（`2602.10656`）§2.3 =
Question Validity Filter + Answer Correctness Filter，附录 A prompt 原文 "If the question can be
answered directly from other information provided (without the genre), output 'no'"；§4.3
"37 行音频/omni 语料场级事实"源自 v2 blob `062c253` 第 112 行、v3–v6 无；Decision-Log 续79 已含
④ 的 round-07 执行注记、**无** ⑤ 的执行注记。

## 九、本轮判定问题

**MAJOR（一项，阻断 owner 落笔）**

1. **新 MAJOR-1（§6.1 A4a 行 / §6.2 `state_t` / §6.5 / §7 K1b）**：v6 的"A4a 等成本 + 探针入等
   预算（A4a 同等计费）"被变化 (c) 拆除后，`A4b − A4a` 这一支的成本控制无人接手——§5.3 阶梯、
   random-matched-cost、`COST_DISPARATE` 三件新装置全部以"基线 ↔ 自适应臂"为轴；K1b② 的 β 权重
   条件与成本无关。A4b 独享逐步候选答案探针（=自一致性采样）与 RE_RESOLVE 轮次，故差分存在未被
   排除的替代解释"只是多打了几倍 core call"。且件内对探针归属自相矛盾（§6.2 共用 `state_t`
   含 `agree(h)`/候选答案漂移 vs §6.5"A4b **独有** core-call 成本"），两读法后果不同而件内未落定
   → 独立性主张的唯一承载差分，其识别作为文本不可判定。§9 变化 (c) 亦未申报这一面。
   修复：探针改两臂共用仪器（推荐）/ `COST_DISPARATE` 镜像到 A4a-A4b 对 / 增 `A4a+` 对照臂，任一。

**MINOR（七项）**

1. random-matched-cost 相对续79⑤ 逐字名录的升格未按 ④ 先例附 Decision-Log 执行注记（实质不违⑤）。
2. matched-cost 被定性为"最低归因对照"却无判据角色（不胜 matched-cost 时件内无规则）。
3. `2303.11366` 列入深读 16 篇但正文从未引用，违 §9 同段"按正文实际引用枚举"自设纪律、与
   `2605.13277` 的处置不对称。
4. §9 保真声明仍不穷尽（另净删 `τ=0.40`/`76`/`316`，均非承重；§4.3 场级事实括注是第二处 v2 恢复
   而变化 (e) 只申报 §5.1）——同型第三次。
5. §1.3"两个 T1 主载体构造过滤主动删除负类"经源核在 AudioRAG-500 上不成立（其 Validity Filter
   删的是"不需要音频属性即可答"的补集）；该载体承载 K3 且可经梯③ 升为主载体。
6. §5.3 阶梯锚点写"自适应臂"而 §7 写"A4a"（A4b 更贵，两读法基线强度不同）；`COST_DISPARATE`
   未声明触发域（阶梯按定义已扫到成本可比，否则构造性空转）。
7. K5-t 生效条件"具备非语义槽需求的载体"未显式绑定前置㈠ 同一阈值；K5-r 称"判定层"而不携判死条款。

**程序性（不计条目）**：round-07 的"未提交"缺陷已闭合（v8 随回应信同 commit `750debf`）；v7 对象
从未入库，故 v7 相关陈述一律以 round-07 记录为据。
