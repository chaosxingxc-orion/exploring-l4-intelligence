# Stage-1C v2 Agentic calibration R2R1 bounded method repair authorization

Date: 2026-07-25

Actor: owner

Exact owner token:

`AUTHORIZE_STAGE1C_V2_AGENTIC_CALIBRATION_R2R1_BOUNDED_METHOD_REPAIR`

## Authorized scope

This token authorizes one immutable R2R1 successor limited to the three load-bearing defects in the
exact R2 independent method review at commit `70786231f8f4a20a117ed6fc9369e00001750fc1`:

1. reject duplicate coder-local object IDs before map insertion, use typed object maps, and make
   every cross-object reference unambiguous;
2. replace reproduction-support placeholder closure with typed paper-observability states and require
   affirmative closure for every task/data/repository/entrypoint/access/terms/evaluator field; and
3. create exact response-submission receipts over canonical raw response bytes, bind both receipts
   into runtime intake and a frozen response root, and verify the bound bytes before agreement.

The repair must be developed test-first, preserve the exact N=56 identities and frozen source
renditions, create new R2R1 schema/contract/artifact identities, run Windows/WSL and deterministic
replay checks, and submit one commit-bound exact package to a fresh independent no-fork reviewer.

## Explicit non-authority

This token does not authorize:

- rewriting R1 or R2 artifacts, replacing a paper/sentinel, adding source exposure or changing 0.85;
- distributing a coder packet, producing an R2R1 coder response, computing agreement or adjudicating;
- promoting a candidate/local asset to a reproduction anchor;
- 320-work mapping, research model/API execution, benchmark metrics, reproduction or prototypes;
- novelty verdicts, Stage-2A, family/branch signature, publication or push; or
- treating repair verification as independent method acceptance.

Only a fresh exact `ACCEPT_AGENTIC_CALIBRATION_R2R1_METHOD_CONTRACT_FOR_CODER_INTAKE` may open the
single already-authorized same-N=56 Sol/Terra recode. Any new defect outside the three bounded classes
returns to the owner rather than being silently absorbed.

## Status after registration

`AGENTIC_CALIBRATION_R2R1_BOUNDED_METHOD_REPAIR_AUTHORIZED_NOT_FROZEN`

No R2R1 coder has been distributed and no R2R1 agreement has been computed at this status.
