# Peer Review — Round 2 (re-review of the revised paper)

> Verification that the round-1 must-fix items are resolved in the revised `main.tex`. Run `wf_d90e0af1-252`, 2026-07-01.


**Outcome: all five reviewers moved from _major revision_ to _minor revision_.** The gating critical items (C1 OSA-2 conditional + Phase-2 conjecture; C2 reward reclassification) and the major honesty items (M3 spread reframing, M4 Lean qualification, M5 no-agentic-recovery claim, M6 convergence-as-design-principle, M10 retrieval-key decoupling) were verified resolved against the revised text. Residual items flagged (M8 JitRL “realizes exactly” wording; the static OSA-3b theorem title; a non-peer-reviewed-preprint caveat on the SkillsBench statistic) were then applied as a final cleanup pass. _(The area-chair agent returned a malformed structured object on this run; the verdict is taken from the five substantive, unanimous re-reviews.)_


---
## Per-reviewer resolution


### Theory/formalization reviewer (focus: C1, M4, M6, constant consistency, OSA acronym)
*Updated recommendation: **minor revision** — 5 resolved / 0 partial / 0 unresolved.*

- **[resolved]** C1 (OSA-2 downgraded to conditional additivity + spread-floor as explicit Phase-2 conjecture; no 'isolation restores spread' gloss; not a proven recovery theorem)  
  Thm gainprod (line 162) now proves only CONDITIONAL additivity 'given' a product base + additively separable, non-degenerate per-block rewards. The dedicated 'Scope: what OSA-2 does and does not establish' paragraph (line 178) is explicit and honest: it 'does not prove that decomposing a fixed, entangled single-model task ... will actually produce such a product structure,' 'does not lower-bound the per-block spread,' 'assumes the favorable structure rather than deriving it from the frozen model,' and calls realizing the hypotheses 'an empirical question, not a theorem ... the central object of our Phase 2 study.' Conjecture 1 (conj:spreadfloor, lines 180-183) makes the per-block spread floor s_0>0 an explicit OPEN conjecture and 'the empirical crux on which the constructive reading of OSA-2 depends.' Line 185 uses 'conjectured mechanism ... provided Conjecture holds.' No 'isolation restores spread' language; no proven-recovery billing.
- **[resolved]** M4 (qualify 'machine-checked': qualitative core sorry-free; quantitative bounds conditional on named hypotheses)  
  Remark 'Honesty about the formalization' (05-theory lines 6-9) and appendix §A 'Verification status' (11-appendix lines 6-38) both state it precisely. The qualitative core (gain_eq, gain_nonneg, flat_no_gain, Zpart_product, gain_product, qstar_product, rollout_deficit, T1, T3) is declared fully sorry-free, with a per-lemma status table (lines 12-35). The three quantitative results are correctly flagged conditional: spread²/(8β) ceiling consumes Hoeffding as an explicit hypothesis (gain_le_of_hoeffding; analytic content proved on PAPER in lem:hoeffding, not in Lean), best-of-N KL bound carries the single documented sorry (klBoN_le_klBoundBoN_TODO), and the O(sqrt log N) regret takes Pinsker as a hypothesis (regret_O_sqrt_log). Line 38: 'we therefore do not claim a fully formalized end-to-end quantitative theory.'
- **[resolved]** M6 (OSA-3 hinge / control law as DESIGN PRINCIPLES not asserted convergence; finite-time guarantee open)  
  OSA-3's 'What OSA-3 proves, and the trust-region analogy (open)' paragraph (05-theory lines 225-226) cleanly separates the two PROVED static facts (rollout_deficit βKL≥0; static factorization qs=q1*⊗q2*) from the dynamical reading: 'Neither statement is a convergence theorem; both describe the geometry of the fixed objective, not the trajectory of any update rule,' the trust-region link is 'an analogy/conjecture, not a theorem,' it explicitly disclaims 'we do not claim that OSA-3a's non-convergence becomes OSA-3b's convergence,' and 'a finite-time monotone convergence guarantee remains open, consistent with our Convergence section.' §6 Convergence reinforces this: output-level finite-N is 'Proven,' agent-level is 'Asymptotic Consistency Under a Trust Region' only, and §6.3 names the open problem as a finite-time monotone-improvement guarantee with the drift condition 'enforced rather than assumed.' Table 1 honestly marks JitRL asymptotic and LATS/UCT 'None transferable.'
- **[resolved]** Minor: 1/(8β) constant internally consistent  
  §5 thm:hoeffding states gain ≤ spread²/(8β) (line 129). Appendix lem:hoeffding derives ψ(1) ≤ (b-a)²/8 = spread²/(8β²) (line 176) then multiplies by β>0 to get spread²/(8β) (line 180). The §5 Assumption form log E[e^{λX}] ≤ λE[X]+λ²ℓ²/8 with λ=1/β, ℓ=spread reproduces the same after ×β. Sandwich 0≤gain≤spread²/(8β) (line 142) consistent. Arithmetic verified correct everywhere.
- **[resolved]** Minor: OSA = Optimization-Space Adequacy only  
  §5 line 4 introduces 'OSA (Optimization-Space Adequacy) throughout'; appendix line 6 reinforces 'Here OSA abbreviates Optimization-Space Adequacy (and only that) throughout.' No competing expansion appears.

  Remaining concerns:
  - Not in my mandate but observed while reading §6: the JitRL attribution still reads 'It proves that the embedding update z'=z+βÂ realizes exactly the KL-constrained optimal policy' (06-convergence line 26). M8 asked that the 'realizes exactly' phrasing on the unverifiable jitrl2026 preprint be softened; the word 'exactly' survives here, though it is heavily caveated immediately after (consistent only in probability, asymptotic, slow-drift precondition). Flagging for the citation-focus reviewer; outside my C1/M4/M6 scope.
  - Cosmetic: Thm qstarprod retains the title 'Per-component tilting reaches the global optimum' (line 205). 'Reaches' reads dynamical, but the body and the OSA-3 summary paragraph make explicit it is only the STATIC factorization of qs; a title like 'assembles the global optimum' would remove any residual ambiguity. Non-blocking.

  *Comment.* All five items in my focus area are resolved. The honesty fixes landed substantively, not cosmetically. C1: OSA-2 is now an explicitly conditional/idealized additivity result with a stand-alone Scope paragraph disclaiming that decomposition produces the assumed product structure, plus Conjecture 1 making the per-block spread floor an open Phase-2 crux — no recovery-theorem framing, no 'isolation restores spread' gloss. M4: the 'machine-checked' claim is now precisely scoped via both a main-text honesty remark and an appendix per-lemma status table that correctly partitions sorry-free qualitative core from the three conditional quantitative results (Hoeffding hypothesis, the lone documented best-of-N sorry, Pinsker hypothesis), with the spread²/(8β) ceiling proved on paper, not in Lean. M6: OSA-3 and the convergence section now present the βKL trust region as a design principle / motivated conjecture, explicitly disclaim a non-convergence→convergence narrative, and state the finite-time monotone guarantee as open. The 1/(8β) constant is arithmetically consistent across §5 and the appendix, and OSA is uniformly Optimization-Space Adequacy. The theory is now presented as supporting — not proving — the agent pivot. The only residuals I noticed (a surviving 'realizes exactly' JitRL attribution under M8, and a slightly dynamical OSA-3b theorem title) are minor and largely outside my mandate; for a proposal judged on honest framing, correct math, and conditional theory, this clears the bar. I would accept on resolution of the M8 wording, which is another reviewer's call.


### Reviewer focused on M3 (spread reframing of Op-B gains), M5 (no agentic-recovery claim), M7 (preliminary-results rigor), M11 (contamination guard)
*Updated recommendation: **minor revision** — 4 resolved / 0 partial / 0 unresolved.*

- **[resolved]** M3 — reframe single-model Operator-B gains (SLURP +0.330 etc.) as single-model high-spread TFRL bounded by the realized reward spread, drop 'asymptotically inert on a single model', not OSA-2 recovery  
  07-feasibility.tex re-titles the paragraph 'Single-model, high-spread Operator-B task-level selection' and states the gains 'reflect a high reward spread S available to a single-model best-of-N search distribution ... fully consistent with---indeed bounded by---the spread^2/(8beta) ceiling on the gain' and 'do not constitute agentic (OSA-2) recovery, and no OSA-2 number is reported in this section.' Table opB caption matches. OSA-1 in 01-intro.tex is reframed as 'the KL-regularized gain is bounded by the realized reward spread'; 'asymptotically inert' is gone (grep: zero hits in main.tex/sections). Minor residual: the paper asserts gains 'sit under the bound' but does not log the per-task realized spread numerically (the round-1 fix suggested logging per-task spread); acceptable for a proposal but the bound is asserted, not demonstrated.
- **[resolved]** M5 — 'early agentic recovery' reworded to 'single-model operator-selection gains'; OSA-2 explicitly not yet measured  
  Abstract: 'We report preliminary single-model operator-selection gains ... agentic recovery (OSA-2) is not yet measured and is Phase-2 work.' C3 in 01-intro.tex: 'report preliminary measurements of flat conditioning and of single-model operator-selection gains. We are explicit that agentic recovery (OSA-2) is not yet measured: validating it is Phase-2 work.' 07-feasibility.tex repeats 'no OSA-2 number is reported here' and bolds that all three tables are single-model. No surviving 'early agentic recovery' phrasing as a claim.
- **[resolved]** M7 — single-seed caveat, no double-counting emotion across Op-A/Op-B, winner's-curse caveat on the 74-config sweep, delete 'dev point 0.513 strengthens'  
  Single seed: Op-A paragraph 'it rests on a single seed (42), and the two CIs touch rather than separate cleanly.' Winner's curse: 'argmax over a 37-layer x 2-pooling (74-configuration) sweep with no multiplicity correction, so the reported +0.097 is subject to a winner's-curse upward bias'; also adds that the paired-delta bootstrap CI is not yet run (Phase-0). Double-counting: Table opB caption states 'The emotion/SER readout gain is reported once, as an Operator-A result in Cref{tab:opA}, and is not repeated here.' 'dev point 0.513 strengthens': grep confirms '0.513', 'dev point', 'strengthens' have zero hits across main.tex and all sections.
- **[resolved]** M11 — contamination guard applied to existing public-dataset numbers, not just the new benchmark  
  07-feasibility.tex adds an explicit contamination caveat: CREMA-D is a 12-sentence closed read set (near-ceiling content accuracy may be memorisation) and 'SLURP, MInDS-14 and URO are public benchmarks that may overlap pretraining corpora.' 09-plan.tex Phase-0 adds a 'Contamination analysis of preliminary public-dataset numbers' paragraph (train-cutoff reasoning, n-gram/transcript overlap, canary/memorization probe) applied to every public-dataset figure, with failing numbers reported as wiring checks. Evaluation Protocol item (6) makes the screen apply to 'every public-dataset number, not only ... the constructed benchmark,' and milestone M1 lists 'screen run' as an exit gate.

  Remaining concerns:
  - M3 nit: the gains are asserted to be 'bounded by' the spread^2/(8beta) ceiling, but the per-task realized reward spread S is never logged, so 'sit under the bound' is a framing claim rather than a shown inequality. With rewards in [0,1] it is plausibly consistent, but a one-line table of per-task spread would convert the assertion into evidence. Not a blocker for a proposal.
  - Op-A emotion gain (+0.097) now correctly hedged, but it still rests on an unrun paired-delta bootstrap CI and a single seed; the paper is honest about this and defers it to Phase-0, which is the right disposition for a proposal but means no positive Op-A result is yet established.

  *Comment.* All four items in my focus (M3, M5, M7, M11) are resolved. The revision is markedly more honest: Operator-B single-model gains are relabeled as high-spread TFRL bounded by the spread^2/(8beta) ceiling and explicitly disclaimed as not constituting OSA-2 recovery; the abstract/C3 now say 'single-model operator-selection gains' and state agentic recovery is unmeasured Phase-2 work; the single-seed, touching-CI, winner's-curse, and emotion double-counting issues are all stated plainly and the 'dev point 0.513 strengthens' sentence is gone; and the contamination screen now covers all public-dataset numbers (CREMA-D closed-read caveat, SLURP/MInDS-14/URO overlap caveat, Phase-0 screen, protocol item 6, milestone M1). For my focus alone I would move to accept; I record minor revision only for the small M3 nit (log per-task realized spread to substantiate 'bounded by the ceiling') and because the overall recommendation must integrate the other panelists' theory/Lean/citation items outside my scope. The honesty fixes in my area landed cleanly.


### Reviewer focused on M10 (retrieval-key decoupling, benchmark realism / DER-SER / listen-only control) and the SER ground-truth portion of C2; sections 07-feasibility, 08-system, 09-plan.
*Updated recommendation: **minor revision** — 5 resolved / 0 partial / 0 unresolved.*

- **[resolved]** M10a: Decouple the retrieval key from the content embedding (do not overload the frozen content embedding with paralinguistics)  
  08-system.tex §subsec:memory adds a dedicated paragraph 'Decoupled retrieval keys: do not overload the content embedding.' It cites the probing evidence (content embedding near-chance on speaker, weakly informative for emotion) and explicitly does NOT route paralinguistic retrieval through the content embedding. Composite key now built from dedicated subkeys: speaker subkey from frozen ECAPA-TDNN/x-vector, emotion subkey from a dedicated SER embedding, content subkey from the omni content embedding; paralinguistic queries retrieve on speaker/SER subspaces, content queries on the content subspace. Framed as a design recommendation with gating weights left to evaluation. The earlier 'index on audio not transcripts' choice is preserved but no longer overloaded.
- **[resolved]** M10b: Benchmark realism -- guard against channel/acoustic fingerprinting; report DER/SER difficulty  
  09-plan.tex 'Construction and its realism risks' explicitly states the validity flaw: stitching single-speaker clips into 'sessions' yields channel-matched exact-audio continuity solvable by 'low-level acoustic/channel fingerprinting rather than by any genuine paralinguistic memory.' It now REQUIRES genuine cross-session variation (re-recording under different device/room/codec/background, or a real repeated-caller corpus) as an admission condition; channel-matched re-keying is allowed only as an explicitly labeled upper-bound/ceiling control. DER and SER error are reported on the constructed set, and 'a constructed set on which DER or SER error is near-trivial is disqualified as an artifact.' Milestone M2 gate encodes this.
- **[resolved]** M10c: Listen-only / transcript-only control as a guard for the read-not-listen shortcut  
  09-plan.tex 'Listen-vs-read control as a primary metric': every paralinguistic query must pass a transcript-only control (a strong text LLM given only gold transcripts must be at chance) before admission; transcript-solvable queries are removed. The audio-vs-transcript gap is reported as a PRIMARY metric, with the explicit statement that a small/zero gap means the benchmark is not measuring listening. Frozen diarization (pyannote) and a frozen SER labeler are used only as proposal tools, audited and human-validated. Milestone M3 gate: 'audio-transcript gap > 0.'
- **[resolved]** C2 (SER ground-truth portion): SER labels from a frozen labeler must not be treated as verifiable ground truth; key-agreement reclassified as surrogate  
  08-system.tex 'Which rewards are verifiable' restricts verifiable rewards to WER/CER, exact-match intent/slot, and speaker-ID accuracy only where ground-truth speaker labels exist; paralinguistic agreement is explicitly excluded. 'Composite keys and their reward status' reclassifies key-agreement as a 'surrogate self-consistency signal' because the stored key is itself a frozen-model estimate (diarization/x-vector/ECAPA/SER), Goodhart-flagged, down-weighted, never the sole admission criterion. 09-plan.tex 'SER-keyed ground truth' gates SER writes/rewards behind a confidence threshold with abstention, and the evaluated subset uses human annotation with multiple annotators and reported inter-annotator agreement (Krippendorff alpha/Fleiss kappa), excluding sub-floor items; exact SER rewards are only against human-agreed labels. WER/exact-match/labeled-SID are foregrounded as load-bearing.
- **[resolved]** Cross-check (M3/M5/M7 visible in 07-feasibility): single-model relabeling, no emotion double-counting, winner's-curse and single-seed caveats  
  Although outside my primary focus, 07-feasibility.tex is consistent with the requested fixes and supports the M10/C2 framing: the Operator-B gains (SLURP +0.330, MInDS-14 +0.132, URO +0.335) are labeled 'Single-model, high-spread Operator-B' and explicitly 'do not constitute agentic (OSA-2) recovery'; the spread^2/(8beta) ceiling is invoked as a bound, not a recovery claim. The emotion/SER readout gain is reported once as Operator-A (tab:opA) and the tab:opB caption states it 'is not repeated here' (no double-counting). Single-seed, touching-CIs, winner's-curse (74-config argmax, no multiplicity correction), and pending paired-delta bootstrap caveats are all stated. Contamination caveat on public datasets is present.

  Remaining concerns:
  - Outside my focus but visible in 07-feasibility.tex line 14: the SkillsBench '+16.2 percentage points' figure is still attributed firmly to skillsbench2026, a 2026 preprint. M8 asked these unverifiable 2026-preprint attributions to be softened; the surrounding prose is now hedged ('in our reading, decisive') but the specific numeric attribution remains crisp. The C2/M10 reviewers should defer to the M8-focused reviewer here.
  - The SER human-annotation pipeline (multi-annotator, IAA floor, confidence gating) is a Phase-1 plan, not yet executed. This is appropriate for a proposal, but no preliminary IAA numbers exist yet, so the SER-ground-truth fix is correct in design but unvalidated in practice -- acceptable given the document is explicitly a proposal.
  - The decoupled-key emotion subkey depends on 'a dedicated SER embedding' whose own reliability is exactly the paralinguistic weakness the paper documents; the design is honest about gating weights being an open empirical question, but the emotion retrieval channel remains the softest part of the architecture.

  *Comment.* All round-1 items in my focus are resolved. M10 is comprehensively addressed across two sections: the retrieval key is genuinely decoupled (dedicated speaker/SER/content subkeys, content embedding reserved for content) in 08-system.tex; benchmark realism is handled with an explicit channel-fingerprinting failure analysis, a genuine-cross-session-variation admission requirement, DER/SER-error reporting with an artifact-disqualification rule, and a listen-vs-read transcript-only control promoted to a PRIMARY metric in 09-plan.tex. The SER-ground-truth portion of C2 is resolved: frozen-labeler SER is no longer treated as verifiable ground truth, key-agreement is reclassified as a surrogate self-consistency signal, and evaluated SER rewards are restricted to human-validated, confidence-gated, IAA-screened labels with WER/exact-match/labeled-SID foregrounded. The framing throughout is honest and appropriately hedged for a proposal (design principles, not theorems; single-model gains, not OSA-2 recovery). I am moving from major revision to minor revision. The only residual item I flag is the still-firm SkillsBench +16.2pp attribution (M8 territory, another reviewer's purview); nothing in my focus blocks acceptance. File paths reviewed: D:/chao_workspace/exploring-l4-intelligence/papers/agent-level-tfrl/sections/07-feasibility.tex, 08-system.tex, 09-plan.tex.


### Reviewer — focus: M9 provenance/reproduce commands, M4 Lean conditional disclosure, citation integrity spot-check (read 07-feasibility, 09-plan, 11-appendix, plus intro/main for cross-checks)
*Updated recommendation: **minor revision** — 2 resolved / 1 partial / 1 unresolved.*

- **[resolved]** M4 — Lean 'machine-checked' qualified; qualitative core sorry-free vs quantitative bounds (spread^2/8beta via Hoeffding, best-of-N KL carrying the one sorry, sqrt-log-N regret via Pinsker) disclosed as CONDITIONAL on named hypotheses  
  Caveat now carried in abstract (01-intro.tex:2 / main.tex:170), C1 (01-intro.tex:31), the section roadmap (01-intro.tex:38), the theory body (main.tex:385,389), the limitations (main.tex:965), and Appendix A. 11-appendix.tex:6-38 adds an explicit per-lemma verification-status table separating sorry-free lemmas from the three conditional ones and naming each undischarged input (Hoeffding hypothesis discharged on paper in Lem. B.1; the one documented order-statistics sorry klBoN_le_klBoundBoN_TODO; Pinsker hypothesis). No remaining unqualified 'machine-checked' overclaim found.
- **[resolved]** M9 — provenance: drop misleading 'cross-team/independent reproduction' language  
  Grep for 'cross-team|independent repro|independent raw' across all sections + main.tex returns nothing. The traceable row is reframed honestly as 'a second configuration (raw schema -> policy on the same frozen model)' (07-feasibility.tex:78). Emotion no longer double-counted: Table tab:opB caption states the SER/readout gain is 'reported once, as an Operator-A result in Tab. opA, and is not repeated here.'
- **[partially resolved]** M9 — provenance: per-table reproduce: command + committed JSON/run-id + datasets.lock.json hash + proofs/tfrl commit printed next to each preliminary table; per-row n/backbone/candidate-budget/reward-fn  
  The actually-misleading overclaim was removed (above), but the requested in-paper artifact provenance for Tables 1-3 is absent: no reproduce: command, no JSON path/MLflow run-id, no datasets.lock.json hash, no proofs/tfrl commit is printed beside any preliminary number (grep of 07-feasibility.tex finds none). The reproduce: discipline is asserted only prospectively for planned milestones (09-plan.tex:50, tab:milestones). Per-row candidate budget N, generative backbone, and reward fn remain unstated for most rows; the MInDS-14 0.25 (Op-A probe) vs 0.883 (Op-B generative) split is now separated by operator paragraph but not explicitly labeled as different measurement modalities, and the lone artifact-traceable row (0.852->0.984) still sits under Operator-B.
- **[unresolved]** M9/M8 — citation integrity spot-check of load-bearing 2026 preprints (jitrl 'realizes exactly', skillsbench '+16.2pp/16-of-84')  
  Both flagged attributions remain verbatim and unsoftened: 06-convergence.tex:26 still says JitRL 'proves that the embedding update z' = z + betaA-hat realizes exactly the KL-constrained optimal policy pi* prop pi_theta exp(betaA)'; 07-feasibility.tex:14 still asserts SkillsBench '+16.2 percentage points ... 16 of 84 skills measurably negative.' No source statement (page/theorem/table) was quoted to substantiate either. Bib IDs (jitrl 2601.18510, skillsbench 2602.12670, coevoskills 2604.01687, omniembedaudio 2604.18360, gsrm 2602.13891) postdate the reviewer knowledge cutoff and could not be independently verified.

  Remaining concerns:
  - Preliminary Tables 1-3 still lack in-paper per-number provenance (reproduce: command, committed JSON/run-id, datasets.lock.json hash, proofs/tfrl commit) — a text/appendix edit, not new experiments; round-1 ask [9]/[280] only partially met.
  - JitRL 'realizes exactly the KL-constrained optimal policy' and SkillsBench '+16.2pp / 16-of-84' are still stated as precise, established attributions to unverifiable 2026 preprints; either soften to hedged paraphrase or quote the source statement.
  - The single artifact-traceable preliminary row (0.852->0.984) remains under Operator-B although round-1 auditing traced its backing JSON to a route=direct_omni embedding-retrieval (Operator-A) run; operator labeling of that row should be reconciled or a genuine generative best-of-N artifact supplied.
  - Per-row N (candidate budget), generative backbone, and reward fn are still missing for most Op-B rows; the ~0.25 vs 0.883 MInDS-14 baselines should be explicitly disambiguated as embedding-probe vs generative-classification.

  *Comment.* Within my focus, the highest-value honesty fixes landed. M4 is fully and cleanly resolved: the 'machine-checked' claim is now scoped identically across abstract, C1, theory body, limitations, and a new per-lemma verification-status table in Appendix A — a reader can see exactly what is sorry-free and what is conditional on Hoeffding/Pinsker/the one order-statistics sorry. On M9, the load-bearing overclaim (the unsupported 'cross-team/independent reproduction' provenance) is gone and re-described accurately, and the emotion double-count is removed. Two residual items keep this short of a clean accept, and both are pure text edits rather than new work: (1) the preliminary tables still carry no in-paper reproduce command / artifact id / lock+proof hash, so the numbers remain untraceable from the page despite §9.6's prospective discipline; and (2) the two specific 2026-preprint attributions flagged in round 1 (JitRL 'realizes exactly', SkillsBench '+16.2pp/16-of-84') are unchanged and still presented as fact without source quotation. Because this is a proposal and the math/Lean honesty core is now correct and well-disclosed, these are minor-revision conditions: add per-table reproduce:/artifact provenance (or explicitly mark the numbers as not-yet-artifact-backed), and either soften or source-quote the two precise citation attributions.


### Reviewer focusing on C2 (reward reclassification), M3 (OSA-1 inert claim), M6 (convergence framing), M8 (citation softening), open-moat hedge, and overall OSA honesty
*Updated recommendation: **minor revision** — 5 resolved / 1 partial / 0 unresolved.*

- **[resolved]** C2 (§8 reward reclassified as surrogate/self-consistency, not verifiable; WER/intent foregrounded)  
  08-system.tex adds a dedicated 'Which rewards are verifiable' paragraph listing only WER/CER, exact-match for intent/slot, and speaker-ID 'only on corpora that carry ground-truth speaker labels' as genuinely verifiable, and states paralinguistic agreement 'do not meet this bar.' The 'Composite keys and their reward status' paragraph explicitly reclassifies key-agreement: 'the stored key is itself a frozen-model estimate ... so "agreement" measures self-consistency of two model passes, not consistency with ground truth. We therefore reclassify key-agreement as a surrogate self-consistency signal,' flagged Goodhart-prone, down-weighted, 'never as the sole admission criterion.' Verifiable channel is foregrounded ('wherever a ground-truth annotation exists, the acceptance gate uses it'). Exactly the requested fix.
- **[resolved]** M3 (drop 'asymptotically inert on a single model'; reframe OSA-1 as gain BOUNDED BY realized reward spread)  
  Intro OSA-1 now reads 'The optimization gain is controlled by the realized reward spread: a Hoeffding argument gives gain <= spread^2/(8beta)'; theory sandwiches it as 0 <= gain <= spread^2/(8beta) (thm:hoeffding + prop:gainnn). The blanket 'asymptotically inert on a single model' phrase is gone from the body; the only surviving 'inert' is an unrelated related-work line about MBR/controlled decoding being 'inert otherwise.' Intro/contributions relabel the single-model gains as 'single-model operator-selection gains' and state agentic recovery (OSA-2) is 'not yet measured.' (Per-task spread logging against the ceiling in the results tables is outside my read scope; flagged for the results-focused reviewer.)
- **[resolved]** M6 (convergence as DESIGN PRINCIPLE, not asserted convergence; finite-time guarantee open)  
  08-system.tex opens by stating 'contractivity here is a design target, not a theorem'; the memory KL-budget is 'a design principle, not a theorem: bounding per-step KL is necessary for stability but we do not claim it is sufficient for finite-time convergence, which remains open'; the skill Pareto non-regression 'We do not claim this guarantees monotone non-degradation ... The construction is engineered to be contractive; certifying that it is remains future work'; the Unified Control Law is presented as 'the architecture's convergence design principle ... Whether the resulting trajectory provably converges ... is the central open question.' 05-theory OSA-3 separates the proved static facts (rollout deficit, qstar factorization) from the trust-region dynamics, stating plainly 'a finite-time monotone convergence guarantee remains open' and 'we do not claim that OSA-3a's non-convergence becomes OSA-3b's convergence.' Consistent with §6/§10.
- **[resolved]** M8 (soften unverifiable 2026-preprint attributions: jitrl2026 'realizes exactly', skillsbench2026 +16.2pp)  
  Full-text grep finds no 'realizes exactly' and no '16.2' anywhere in main.tex or sections. JitRL is now described qualitatively ('no-gradient credit assignment', 'characterizes when inference-time self-correction provably converges', and in the conjecture-framed OSA-3 'JitRL's no-gradient credit-assigned tilt' inside an explicitly labeled analogy/conjecture). SkillsBench is cited qualitatively ('curated skill sets substantially outperform self-generated ones, because unverified skills accumulate noise')—no percentage carried. The precise numeric/theorem-level overclaims are removed.
- **[partially resolved]** Open-moat hedge  
  Hedge is propagated to first use in the intro ('To our knowledge no speech-native ... agent of this form exists; the omni-embedding-as-memory pairing is, at present, an unfilled slot, to our knowledge') and the abstract avoids the bald 'moat' language. Residual: the related-work subsection title remains 'Speech Agents and the Open Moat' and its body still asserts 'The consequence is an open moat,' and I found no dated/reproducible literature-search citation making the negative existence claim auditable (the M10 'absence-of-evidence' ask). Cosmetic for a proposal but not fully closed.
- **[resolved]** Overall: OSA theory now SUPPORTS (not proves) the agent pivot  
  05-theory is now exemplary on this axis. OSA-2 (thm:gainprod) carries an explicit 'Scope: what OSA-2 does and does not establish' paragraph: it proves additivity GIVEN a product base + non-degenerate separable rewards, and explicitly does NOT show decomposition converts a flat monolithic reward into non-degenerate per-component rewards, nor lower-bounds per-block spread. The spread-floor is stated as an explicit Conjecture (conj:spreadfloor, 'open conjecture ... empirical crux ... not something the present theory establishes'). No 'isolation restores spread' gloss survives. The Lean honesty Remark cleanly separates the sorry-free qualitative core from the conditional quantitative bounds (Hoeffding ceiling, the one documented sorry in the best-of-N KL bound, Pinsker regret). The theory is correctly positioned as explaining why agentic decomposition is the natural place to look, not as a proof that it works.

  Remaining concerns:
  - Cosmetic residual on the open-moat claim: the related-work subsection is still titled 'Speech Agents and the Open Moat' and asserts 'an open moat' in body, and there is no dated/reproducible literature-search citation backing the negative existence claim (M10 absence-of-evidence ask). Recommend retitling to 'an unfilled slot, to our knowledge' and adding an auditable search note.
  - M3's quantitative half (logging per-task realized spread in the results tables and showing the single-model Operator-B gains sit under the spread^2/(8beta) ceiling) is in the results/feasibility sections outside my assigned read scope; the framing fixes I can see are correct, but a results-focused reviewer should confirm the tables themselves relabel and bound-check the +0.330-type gains and avoid double-counting emotion (M5/M7).
  - Math sanity within my scope is clean: the 1/(8beta) constant is consistent between intro, abstract, and thm:hoeffding; OSA = Optimization-Space Adequacy only throughout.

  *Comment.* Within my assigned focus the honest-framing fixes landed comprehensively and convincingly. C2, M3 (qualitative), M6, and M8 are fully resolved with verbatim-checkable text; the OSA theory is now presented as supporting—not proving—the agent pivot, with OSA-2's spread-floor demoted to an explicit Phase-2 conjecture and OSA-3's convergence reduced to a labeled analogy. The reward reclassification in §8 is exactly the requested surrogate/self-consistency reframe, with the verifiable channel foregrounded. The only items keeping me at 'minor' rather than 'accept' are cosmetic (the surviving 'Open Moat' section title and the missing auditable lit-search) plus deference to the results-focused reviewer on the M5/M7 table-level checks I did not read. For a proposal (theory + feasibility + plan + preliminary results), the bar of honest framing, correct math, and citation integrity is now met. I would accept after the open-moat title/lit-search touch-up.
