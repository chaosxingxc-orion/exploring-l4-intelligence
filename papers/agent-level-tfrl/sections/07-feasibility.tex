\section{Feasibility Analysis}
\label{sec:feasibility}

We now argue that the agent-level program of this work is not only well-posed but already \emph{instantiable} from off-the-shelf \emph{frozen} components, and that our own preliminary measurements both motivate and sharply \emph{constrain} the design. The argument proceeds in four steps: we identify the two classes of frozen omni models with the two structural roles an episodic agent requires (\Cref{sec:feas:components}); we show that a single verifiable-reward acceptance gate furnishes the control law shared by memory and skills (\Cref{sec:feas:gate}); we state an honest novelty delta against a crowded mechanism literature (\Cref{sec:feas:novelty}); and we present in-house results that locate the operating regime empirically (\Cref{sec:feas:prelim}).

We state at the outset the organising principle that the theory section makes precise and that our measurements bear out. The optimization gain of training-free RL---the KL-regularised tilting $\qs \propto \qo\,\exp(\Rwd/\beta)$---is governed by the reward \emph{range} $\spread=\max\Rwd-\min\Rwd$ over $\mathrm{supp}(\qo)$ (OSA-1, Lean-checked), whose leading-order discriminator is the reward variance $\mathrm{Var}_{\qo}(\Rwd)/(2\beta)$; the OSA-1 gain identity is $\gain=\beta\,\KL(\qo\,\|\,\qs)$. This is a property of the \emph{reward structure}, not of model class. A flat reward (e.g.\ instruction-conditioning of a frozen embedding read-out) gives vanishing gain $\gain$; a high-range verifiable reward (e.g.\ task-level verifiable selection) gives large gain \emph{even on a single frozen model}. Context-isolated composition makes gains \emph{additive} across blocks (OSA-2, \texttt{gain\_product}), and every non-degenerate block contributes \emph{strictly positive} gain (\texttt{gain\_pos\_of\_nonconstant}: reward non-constant $\Rightarrow \gain>0$, sorry-free in Lean). But isolation \emph{per se} adds nothing: \texttt{qstar\_product} (the OSA-2 factorization) proves the context-isolated optimum \emph{equals} the monolithic single-model optimum on the product reward $\Rwd=\sum_i \Rwd_i$. Consequently the high-range single-model gains we report below are \emph{single-model} gains, not evidence that agentic decomposition recovers anything: the only \emph{new} gain comes from introducing additional \emph{non-degenerate} verifiable rewards, and whether such genuinely new per-block rewards exist for a given factor on a given frozen model is an \emph{empirical} question (Conjecture~\ref{conj:spreadfloor}, the per-block spread floor), untested at the agentic level here. Our preliminary results answer it affirmatively for content/intent on a single frozen model, and find no significant emotion gain on the one content bi-encoder we probed; we report both plainly.

\subsection{The Two Omni Classes as Agent Components}
\label{sec:feas:components}

The decisive enabling observation is that the contemporary omni-model ecosystem ships, off the shelf, the two computational primitives an episodic agent needs, and that both can be used \emph{frozen}. On one side, \emph{vector-omni bi-encoders} map an audio (or audio--text) input to a fixed embedding in a shared retrieval space \citep{omniembed2025,omniembedaudio2026}; their natural role is the agent's \emph{memory and index}, the read/write substrate over past episodes, as speech-native retrieval-augmented systems already use them \citep{wavrag2025,speechrag2024,voxrag2025}. On the other side, \emph{generative thinker--talker} omni models map an input to a token sequence under a decoding policy \citep{qwen3omni,qwen25omni,salmonn2023}; their natural role is the agent's \emph{policy and skill executor}. Both classes are pretrained, publicly released, and, for our purposes, \emph{frozen}: the action lives entirely in the inference-time degrees of freedom $\cZ$, partitioned into the embedding/retrieval operator $\cZA$ realised by the bi-encoder and the generative operator $\cZB$ realised by the thinker--talker. This separation lets us treat memory construction (Operator-A) and skill execution (Operator-B) as two faces of a single search distribution $\qz$ over $\cZ$, optimised against the same reward $\Rwd$ without touching weights or architecture.

We must, however, be honest about what one \emph{specific} frozen omni \emph{content bi-encoder} can and cannot index, because our own probes (\Cref{sec:feas:prelim}) constrain it. On a single bi-encoder, \texttt{omni-embed-nemotron-3b}, the content axis is strong while the paralinguistic axes are not. Speaker identity is recoverable at \emph{near chance} from this embedding at every layer ($\approx 0.04$ on a $91$-way task, chance $0.011$); since it is a \emph{content} embedding, near-chance speaker recovery is close to definitional rather than surprising, and emotion is recoverable only weakly (with no significant gain at the across-seed level; see below). An index built solely on this omni \emph{content} embedding would therefore retrieve on content, not on who-spoke or how-they-felt. We accordingly reframe the memory honestly: this frozen omni embedding is reserved for the \emph{content} channel, while paralinguistic memory keys are supplied by classic, frozen, special-purpose speech encoders (an ECAPA-TDNN--style speaker embedder; a dedicated SER read-out). We do \emph{not} generalise this negative to frozen omni models as a class, and we emphasise that it concerns a bi-encoder \emph{probe}, not the generative Operator-B policy (which we do not test for paralinguistics). Feasibility thus reduces to whether a \emph{control law} can compose a frozen omni policy with a heterogeneous frozen-encoder memory productively---not to whether the parts can be built, and not to a claim that one omni embedding carries every factor.

\subsection{The Verifiable-Reward Acceptance Gate as the Control Law}
\label{sec:feas:gate}

The control law is a verifiable-reward \emph{acceptance gate}: a candidate memory write or skill invocation is admitted only if it improves a verifiable reward on held-out probes, and is otherwise rejected. This single mechanism governs both faces of $\cZ$. The case for making the gate load-bearing is empirical and, in our reading, strong. It rests substantially on \citet{skillsbench2026}, whose statistics we verified against the source (an average of $+16.2$ points, ranging $+4.5$ to $+51.9$, with $16/84$ skills negative), and we triangulate it against the broader agent-stability literature below. \textsc{SkillsBench} reports that \emph{curated} skills raise agent success by $+16.2$ percentage points, whereas \emph{self-generated} skills contribute essentially zero in aggregate, with $16$ of $84$ skills measurably \emph{negative} \citep{skillsbench2026}. The lesson is not that self-evolution is futile but that unfiltered self-evolution is: without an acceptance test, an append-only memory or skill library accrues as much harm as benefit, a failure mode echoed across the agent-stability literature \citep{expfollowing2025,ace2025,remembering2026,reflexion2023}. The gate is precisely the missing filter. When ground-truth labels are scarce, a surrogate verifier can stand in for the reward without collapsing the guarantee, as co-evolved skill--verifier pairs demonstrate \citep{coevoskills2026}; and for speech specifically the verifiable rewards already exist, from paralinguistic-aware speech reward models \citep{gsrm2026,paras2s2025} to the WER/exact-match/intent rewards reused throughout this series. We therefore adopt the gate as the one control law shared by $\cZA$ (admit a memory entry) and $\cZB$ (admit a skill or decoding action), with the surrogate verifier as the fallback when labelled probes run out. This is the same KL-regularised tilting that underwrites best-of-$N$ and reward-guided decoding \citep{korbak2022kl,mudgal2024controlled,beirami2024bon}: maximising $\Fobj(q)=\Exp_{q}[\Rwd]-\beta\,\KL(q\,\|\,\qo)$ with optimum $\qs \propto \qo\,\exp(\Rwd/\beta)$, here lifted from token decisions to episode-level admission decisions. Its gain is exactly the reward-range/variance quantity of \Cref{sec:feasibility}: a high-range admission reward makes the gate productive; a flat one makes it inert.

\subsection{Novelty Delta}
\label{sec:feas:novelty}

We state the contribution honestly. The \emph{mechanism}---self-evolving episodic memory plus a growing skill library refined by feedback---is crowded: reflective self-improvement \citep{reflexion2023,expel2023}, library-building agents \citep{voyager2023,awm2024,museautoskill2026}, and gradient-free inference-time credit assignment \citep{jitrl2026} all occupy adjacent ground. Our claim is not the mechanism in the abstract. The unfilled slot is the specific composition: a \emph{frozen omni policy} (Operator-B) and a \emph{frozen-encoder memory} (Operator-A) driven by \emph{episodic self-evolution} under a \emph{verifiable speech reward}, with no weight updates anywhere in the loop. We are explicit that this memory is heterogeneous and \emph{not} a single-vector omni store: as established above and measured below, the one omni content bi-encoder we probed is at chance on speaker and weak on emotion, so we use it for the content channel only and delegate paralinguistic keys to classic frozen speech encoders. The closest priors fall short on the all-frozen axis. Trainable instruction embedders such as \textsc{bge-en-icl} require gradient training to acquire their in-context behaviour \citep{bgeenicl2024}, violating the frozen-memory constraint; few-shot--emergent audio models such as \textsc{MiMo-Audio} obtain their capability through pretraining rather than through inference-time activation of an already-frozen model \citep{mimoaudio2025}. Neither realises the all-frozen, reward-gated, speech-verifiable agent loop. To our knowledge, this composition has not been demonstrated, and we make the corresponding ``first, to our knowledge'' claim while being explicit that each ingredient in isolation has prior art---and that the load-bearing novelty is the verifiable-reward composition, not any claim that a single omni embedding affords every factor.

\subsection{Preliminary Empirical Evidence}
\label{sec:feas:prelim}

Our in-house measurements locate the operating regime along the single axis the theory identifies: the reward range $\spread$ of each (action-space, reward) pair. We organise the evidence by \emph{channel}. A \textbf{content/intent ``read'' channel}---task-level verifiable selection over a single frozen model---exhibits high range and large gains. A \textbf{paralinguistic ``listen'' channel}---emotion and speaker read-out from one frozen content bi-encoder via a linear/kNN probe---exhibits low range; for these factors, on this bi-encoder, our probe evidence does \emph{not} support a per-block spread floor. All numbers below are our own preliminary results on a frozen \texttt{omni-embed-nemotron-3b} with kNN probes and bootstrap confidence intervals (CREMA-D dev$600$/test$300$ and MInDS-14 dev$280$/test$257$ for the probe channel; SLURP $n{=}500$, MInDS-14 $n{=}180$, URO $n{=}200$ for the Operator-B channel); they are reported verbatim and labelled preliminary. We flag a contamination caveat: CREMA-D content is a $12$-sentence closed read set, so near-ceiling content accuracy ($\approx 1.00$) may partly reflect memorisation of fixed transcripts, and SLURP, MInDS-14 and URO are public benchmarks that may overlap pretraining corpora; the single-model gains below should be read with that risk in mind. We stress at the outset that every Operator-B number is a \emph{single-model} result; by \texttt{qstar\_product} a context-isolated decomposition would reach the same optimum, so none of these gains is evidence of agentic recovery.

\paragraph{Single-model conditioning: no effect larger than the detectable minimum.}
\Cref{tab:cremad-matrix} reports test probe accuracy for the frozen embedding under four instruction-conditioning regimes, crossed against three read-out factors. The same frozen embedding simultaneously supports near-perfect content read-out ($\approx 1.00$), weak emotion read-out ($\approx 0.36$), and near-chance speaker read-out ($\approx 0.04$; chance on this $91$-way task is $0.011$). Instruction conditioning does not measurably move the factor accuracies: the emotion delta from baseline to emotion-instruction is $+0.007$ with overlapping CIs, the speaker delta is $-0.003$, and the conditioning matrix is not diagonally dominant. We report this with the honesty its statistical power warrants. At $n=300$ the study is \emph{underpowered}; we therefore report it as \emph{no effect larger than the minimum detectable effect (MDE) at $n=300$}, not as a positive ``OSA-1 signature'' and not as a proof that conditioning is exactly inert. A sweep over all $37$ hidden states and two pooling read-outs leaves speaker near chance at \emph{every} layer (full-sequence best $0.043$ at L36; audio-token best $0.043$ at L32), with content saturating early (full-seq $0.990$ at L24) and emotion peaking only weakly (full-seq $0.400$ at L0). Whatever range instruction-conditioning offers is below what this probe can resolve; we draw no stronger conclusion.

\begin{table}[t]
\centering
\caption{In-house preliminary results. CREMA-D conditioning$\times$factor TEST probe accuracy (frozen \texttt{omni-embed-nemotron-3b}, seed $42$). Instruction conditioning produces no effect larger than the MDE at $n=300$ (overlapping CIs, \texttt{diagonal\_dominant}$=$False)---an underpowered null, not a proven equivalence. Single-model only.}
\label{tab:cremad-matrix}
\begin{tabular}{lccc}
\toprule
Conditioning & Content & Emotion & Speaker \\
\midrule
Baseline              & 0.997 & 0.360 & 0.040 \\
Content-instruction   & 1.000 & 0.333 & 0.043 \\
Emotion-instruction   & 1.000 & 0.367 & 0.037 \\
Speaker-instruction   & 1.000 & 0.367 & 0.033 \\
\midrule
Chance                & 0.08  & 0.17  & 0.011 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Paralinguistic ``listen'' channel: no significant emotion gain (null) and near-chance speaker.}
For the flagship's paralinguistic factors the per-block range the agent would need is small, and on this content bi-encoder it is statistically null. Choosing the read-out itself---an Operator-A ($\cZA$) action over layer and pooling---can extract an emotion gain on \emph{some} splits, but a rigorous multi-seed re-run shows no significant gain at the across-seed level, and it forces us to retract an earlier over-claim. We previously reported a single-seed $+0.097$ (attentive-statistics pooling at L16 versus mean-pooling at L0). That figure was the \emph{best seed under oracle TEST-layer selection}---a winner's-curse / selection leak, since the test layer was chosen on the test set. We have since corrected the protocol on three counts: (i) \emph{dev}-layer selection (both the base mean-pool layer and the selected attentive-stats layer are chosen on dev and frozen before test evaluation); (ii) a \emph{paired}-delta bootstrap $95\%$ CI computed directly on the per-example improvement; and (iii) five seeds $\{42,7,123,2024,31337\}$. \Cref{tab:emotion-multiseed} reports the result: across the five seeds the mean improvement is $\Delta=+0.037$ with an across-seed $95\%$ $t$-CI of $[-0.043,+0.116]$, which \emph{spans zero}. The headline reading is therefore \emph{no significant emotion gain at the across-seed level (a null)}, and the previously reported $+0.097$ is reproduced only as the best of five seeds under the corrected protocol.\footnote{For completeness: the per-seed paired-delta CI excludes $0$ in $2$ of the $5$ seeds (and two seeds are nominally negative). This $2/5$ vote count is not the headline; the across-seed $t$-CI above is the appropriate aggregate, and it is a null.} Speaker read-out is worse: it stays near chance at all $37$ layers ($\approx 0.04$, chance $0.011$), which for a content embedding is near-definitional, so this embedding encodes essentially no recoverable speaker identity. For paralinguistics, then, our \emph{probe} evidence on this one content bi-encoder does \emph{not} support a non-degenerate per-block reward: Conjecture~\ref{conj:spreadfloor} is not supported here. We stress the scope: this is a linear/kNN probe of a frozen content bi-encoder; it does \emph{not} bound the proposed generative Operator-B policy, which we have not tested for paralinguistics. Committed artifact: \texttt{projects/speech-mllm-omni-embedding-rl/\_repro/emotion\_pool\_paired\_v2.json}; reproduce with \texttt{PP\_SEEDS=42,7,123,2024,31337 python scripts/pool\_method\_probe\_paired.py}.

\begin{table}[t]
\centering
\caption{In-house preliminary results, \emph{corrected protocol}. CREMA-D emotion: mean-pool baseline vs attentive-statistics pooling, both layers \emph{dev}-selected, paired-delta bootstrap $95\%$ CI on TEST, five seeds. The earlier single-seed $+0.097$ was the best seed under \emph{oracle test-layer} selection; under dev-selection the across-seed $95\%$ $t$-CI is $[-0.043,+0.116]$, which spans zero: no significant emotion gain at the across-seed level (a null). Paralinguistic listen channel on one content bi-encoder; Conjecture~\ref{conj:spreadfloor} not supported here.}
\label{tab:emotion-multiseed}
\begin{tabular}{lllccrcl}
\toprule
Seed & base (dev-sel) & sel (dev-sel) & acc$_\text{base}$ & acc$_\text{sel}$ & $\Delta$ & paired $95\%$ CI & sig. \\
\midrule
$42$    & audio-mean L0 & audio\_attn L16 & 0.400 & 0.497 & $+0.097$ & $[+0.027,+0.167]$ & SIG \\
$7$     & audio-mean L0 & audio\_attn L0  & 0.423 & 0.370 & $-0.053$ & $[-0.117,+0.010]$ & n.s. \\
$123$   & audio-mean L0 & audio\_attn L16 & 0.390 & 0.473 & $+0.083$ & $[+0.013,+0.150]$ & SIG \\
$2024$  & audio-mean L0 & audio\_attn L16 & 0.493 & 0.487 & $-0.007$ & $[-0.077,+0.063]$ & n.s. \\
$31337$ & audio-mean L0 & audio\_attn L16 & 0.400 & 0.463 & $+0.063$ & $[-0.010,+0.130]$ & n.s. \\
\midrule
mean    & --- & --- & --- & --- & $+0.037$ & across-seed $95\%$ $t$-CI $[-0.043,+0.116]$ (spans $0$; null) & --- \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Content/intent ``read'' channel: high range, large single-model gains.}
The contrast is task-level Operator-B ($\cZB$) selection---choosing among generative candidates of a \emph{single frozen model} under a verifiable reward, the best-of-$N$ training-free-RL move of this series. Here the reward range is large and the gains are well separated \emph{even on a single frozen model} (\Cref{tab:opB}); by \texttt{qstar\_product} a context-isolated decomposition would not improve on this single-model optimum, so we read these as single-model gains, not agentic recovery. We give the exact selection rule and slice for each pair. On \textbf{SLURP intent} ($n{=}500$ test slice), the baseline is a basic-label intent retriever (raw audio query against the bare intent-name text) at $0.550$; admitting boundary-card candidate schemas (name $+$ description $+$ examples $+$ boundary notes) under the verifiable intent-match reward raises this to $0.880$ ($+0.330$, paired-bootstrap CI $[0.288,0.374]$). On \textbf{MInDS-14 intent} ($n{=}180$ slice), our canonical pair applies the same basic$\to$boundary-card selection rule with the policy schema, improving $0.852\!\to\!0.984$ ($+0.132$, CI $[0.082,0.187]$); an \emph{alternative configuration} (basic-label $\to$ tool-specific intent on the same frozen model) gives $0.883\!\to\!0.972$ ($+0.089$) but was not re-run under the paired bootstrap and carries no CI, so we report it only as an alternative, not as the canonical MInDS result. On \textbf{URO spoken-QA} ($n{=}200$ slice), the baseline is raw target-text candidates at $0.380$; substituting target boundary-card candidates with the raw query (policy grounding) raises this to $0.715$ ($+0.335$, CI $[0.265,0.405]$). A subsequent conservative low-margin rerank ($\tau=0.02$) is an \emph{improve-only} selector by construction---it admits a re-ranking only on a strict verified improvement---so its ``$0$ regressions'' is a structural (definitional) property of the gate, not an empirical robustness finding; it accepts $26$ fixes over the $200$-item slice and lifts $0.715\!\to\!0.845$ (net $+0.130$), which we present as such, with no separate robustness claim. We reconcile the provenance honestly. The project's own \texttt{docs/theory.md} reports a \emph{different} boundary-card pipeline for SLURP ($0.522\!\to\!0.894$, $+0.372$, which subsequently showed a $0.894\!\to\!0.880$ regression under a later change) together with several MInDS variants; we treat those as \emph{alternative configurations / pipelines not re-run here} and report \emph{one canonical baseline$\to$after pair per task} under the fixed selection rules above. We do \emph{not} present a single clean $+0.330$ as if it were the unique result. What these numbers establish---and all they establish---is that a high-range verifiable reward yields large gain on a single frozen model, exactly as OSA-1 predicts. We emphasise that this high range is demonstrated for \textbf{content/intent only}.

\begin{table}[t]
\centering
\caption{In-house preliminary results. Content/intent read channel: single-model Operator-B ($\cZB$) best-of-$N$ training-free-RL gains (seed $42$, paired bootstrap). Task-level verifiable selection within one frozen model yields large, well-separated improvements, in contrast to the flat conditioning of \Cref{tab:cremad-matrix} and the null paralinguistic gain of \Cref{tab:emotion-multiseed}. One canonical baseline$\to$after pair per task under a fixed selection rule (SLURP/MInDS: basic-label $\to$ boundary-card schema; URO: raw target text $\to$ boundary-card $+$ raw query); the MInDS basic$\to$tool-specific variant and the \texttt{docs/theory.md} SLURP $0.522\!\to\!0.894$ pipeline (with a later $0.894\!\to\!0.880$ regression) are noted as alternative configurations, not re-run here and not canonical. The conservative-rerank ``$0$ regressions'' is a structural property of an improve-only gate (definitional). Single-model only.}
\label{tab:opB}
\begin{tabular}{lcccc}
\toprule
Task ($n$) & Baseline & After Op-B & $\Delta$ (CI) & Note \\
\midrule
SLURP intent ($500$)            & 0.550 & 0.880 & $+0.330$ \,[0.288,0.374] & canonical \\
MInDS-14 intent ($180$)         & 0.852 & 0.984 & $+0.132$ \,[0.082,0.187] & canonical \\
\quad alt.\ config (basic$\to$tool-spec.) & 0.883 & 0.972 & $+0.089$ \,(no CI) & alternative config \\
URO spoken-QA ($200$)           & 0.380 & 0.715 & $+0.335$ \,[0.265,0.405] & canonical \\
\quad $+$ conservative rerank ($\tau{=}0.02$) & 0.715 & 0.845 & $+0.130$ & improve-only (0 regr.\ by constr.) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Reading the evidence.}
The picture is consistent and, in one direction, humbling. The optimization gain tracks the reward range $\spread$ of the (action-space, reward) pair, \emph{not} the model class: a single frozen model already affords large gains under a high-range verifiable reward (content/intent: $+0.330$ SLURP, $+0.132$ MInDS, $+0.335$ URO), while a flat reward (instruction-conditioning) affords none we can detect at $n=300$. For content and intent this supports composing verifiable per-block rewards across operators and episodes: by \texttt{gain\_pos\_of\_nonconstant} each non-degenerate block adds strictly positive gain, and by \texttt{gain\_product} these add. But the same theory is honest about its precondition: \texttt{qstar\_product} shows that context-isolation \emph{per se} buys nothing---the context-isolated optimum equals the monolithic single-model optimum---so the content/intent gains above are single-model gains, and whether \emph{agentic decomposition} adds anything beyond them can only be settled by introducing genuinely new non-degenerate per-block rewards, which we have not tested. For the flagship's \emph{paralinguistic} factors our probe evidence does not deliver a non-degenerate per-block reward on the one content bi-encoder we examined---the across-seed emotion gain is a null (mean $\Delta=+0.037$, $95\%$ $t$-CI $[-0.043,+0.116]$, spans $0$) and speaker is near chance at every layer---so Conjecture~\ref{conj:spreadfloor} is not supported there. This is a statement about a frozen content bi-encoder under a linear/kNN probe; it does \emph{not} bound the untested generative Operator-B policy and does \emph{not} generalise to frozen omni models as a class. We therefore pursue the content/intent channel as a single-model best-of-$N$ result, reserve the agentic-decomposition question for future genuinely-new rewards, and explicitly decline a paralinguistic per-block claim, where this omni content embedding does not afford a load-bearing key and an index built on it would not perform load-bearing paralinguistic retrieval. We report all intervals, label these numbers preliminary, and state the negative result as plainly as the positive one: the design is motivated where the range is high and constrained where it is not.